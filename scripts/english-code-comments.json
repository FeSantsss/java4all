{
  "// métodos concretos também são permitidos": "// concrete methods are also allowed",
  "// roda via super() nas subclasses": "// runs via super() in subclasses",
  "// Forma forma = new Forma();  // ❌ erro de compilação": "// Shape shape = new Shape();  // ❌ compilation error",
  "// main: new ProcessadorPedidoPix().processar();": "// main: new ProcessorPedidoPix().processar();",
  "// final: subclasses não podem mudar a ordem": "// final: subclasses cannot change the order",
  "(com detalhes habilitados)": "(with details enabled)",
  "// Forma forma = new Forma();  // ❌ erro de compilação\n// Circulo c = new Circulo(2); // ✅ ok": "// Shape shape = new Shape();  // ❌ compilation error\n// Circle c = new Circle(2); // ✅ ok",
  "{\"status\": \"UP\"}": "{\"status\": \"UP\"}",
  "// Circulo c = new Circulo(2); // ✅ ok": "// Circle c = new Circle(2); // ✅ ok",
  "// sem corpo": "// without body",
  "<!-- pom.xml -->": "<!-- pom.xml -->",
  "-- pom.xml -->": "-- pom.xml -->",
  "{\n  \"status\": \"UP\",\n  \"components\": {\n    \"db\": {\"status\": \"UP\"},\n    \"diskSpace\": {\"status\": \"UP\"}\n  }\n}": "{\n  \"status\": \"UP\",\n  \"components\": {\n    \"db\": {\"status\": \"UP\"},\n    \"diskSpace\": {\"status\": \"UP\"}\n  }\n}",
  "# a imagem precisa conter wget; se não contiver, use um probe disponível\n# ou faça a checagem externamente. Não adicione curl sem ajustar a imagem.": "# the image must contain wget; if it does not, use an available probe\n# or check externally. Don't add curl without adjusting the image.",
  "//localhost:8080/actuator/health/readiness || exit 1\"]": "//localhost:8080/actuator/health/readiness || exit 1\"]",
  "# docker-compose.yml -- usando o health check para o Compose saber quando a API está pronta:": "# docker-compose.yml -- using health check so Compose knows when the API is ready:",
  "# a imagem precisa conter wget; se não contiver, use um probe disponível": "# the image must contain wget; if it does not, use an available probe",
  "# ou faça a checagem externamente. Não adicione curl sem ajustar a imagem.": "# or check externally. Don't add curl without adjusting the image.",
  "//localhost:8080/actuator/health\"]": "//localhost:8080/actuator/health\"]",
  "// \"Livro\"": "// \"Book\"",
  "// reflection também permite criar objetos e chamar métodos dinamicamente,": "// reflection also allows you to create objects and call methods dynamically,",
  "// precisa existir em runtime para ser lida via reflection": "// must exist at runtime to be read via reflection",
  "// reflection também permite criar objetos e chamar métodos dinamicamente,\n// sem \"new\" explícito no código -- é assim que o Spring instancia seus beans:": "// reflection also allows you to create objects and call methods dynamically,\n// without explicit \"new\" in the code -- this is how Spring instantiates its beans:",
  "// \"atributo\" da anotação, com valor padrão": "// \"attribute\" of the annotation, with default value",
  "// chama servico.processar() dinamicamente": "// calls servico.processar() dynamically",
  "// só pode ser usada em métodos": "// can only be used in methods",
  "// array de interfaces implementadas": "// array of implemented interfaces",
  "// a partir de uma instância já existente": "// from an existing instance",
  "// quando você já conhece o tipo em compilação": "// when you already know the type in compilation",
  "// pelo NOME, como String -- usado por drivers JDBC e frameworks": "// by NAME, as String -- used by JDBC drivers and frameworks",
  "// sem \"new\" explícito no código -- é assim que o Spring instancia seus beans:": "// without explicit \"new\" in the code -- this is how Spring instantiates its beans:",
  "// \"com.felipy.Livro\" -- nome totalmente qualificado": "// \"com.felipy.Livro\" -- fully qualified name",
  "// a classe pai (Object, se não houver extends explícito)": "// the parent class (Object, if there are no explicit extends)",
  "// públicos, incluindo herdados": "// public, including inherited",
  "// altera mesmo sendo private": "// changes even though it is private",
  "// todos os construtores declarados": "// all constructors declared",
  "// delega para o objeto real": "// delegate to the real object",
  "// [LOG] Antes de executar\n// Executando lógica de negócio\n// [LOG] Depois de executar": "// [LOG] Before executing\n// Executing business logic\n// [LOG] After executing",
  "// [LOG] Antes de executar": "// [LOG] Before executing",
  "// chamando um método privado dinamicamente:": "// calling a private method dynamically:",
  "// código \"injetado\" ao redor da chamada": "// code \"injected\" around the call",
  "// false -- e existe isEnum(), isArray(), isAnnotation()...": "// false -- and there is isEnum(), isArray(), isAnnotation()...",
  "// criando um objeto sem \"new\" explícito no código-fonte:": "// creating an object without explicit \"new\" in the source code:",
  "// \"quebra\" o encapsulamento -- use com responsabilidade!": "// \"breaks\" encapsulation -- use responsibly!",
  "// o mesmo padrão vale para métodos e construtores:": "// the same pattern applies to methods and constructors:",
  "// campos: getFields() só PUBLIC (inclusive herdados); getDeclaredFields() TODOS, só desta classe": "// fields: getFields() only PUBLIC (including inherited); getDeclaredFields() ALL, only for this class",
  "// todos os declarados NESTA classe, qualquer modificador": "// all declared in THIS class, any modifier",
  "// [LOG] Depois de executar": "// [LOG] After executing",
  "// uso:": "// usage:",
  "// a \"porta\"": "// the \"door\"",
  "// não obrigatório": "// not required",
  "// lança IllegalStateException: \"nome\" é null": "// throws IllegalStateException: \"name\" is null",
  "// Executando lógica de negócio": "// Executing business logic",
  "// altera o OBJETO apontado (visível fora!)": "// changes the OBJECT pointed to (visible outside!)",
  "// novo array de primitivos": "// new array of primitives",
  "// Spring Data, capítulo 45": "// Spring Data, chapter 45",
  "// altera só a cópia local": "// only changes the local copy",
  "// inspecionar(new Livro(\"1984\", \"Orwell\", 328));": "// inspect(new Book(\"1984\", \"Orwell\", 328));",
  "// adaptador concreto -- É aqui que JPA aparece, isolado do núcleo:": "// concrete adapter -- This is where JPA appears, isolated from the core:",
  "// núcleo de domínio -- NÃO sabe que existe JPA, HTTP, ou Kafka:": "// domain core -- DOES NOT know there is JPA, HTTP, or Kafka:",
  "// só troca para o que a cópia LOCAL da referência aponta": "// only swaps what the LOCAL copy of the reference points to",
  "// se a versão atual for 8: 412 Precondition Failed": "// if current version is 8: 412 Precondition Failed",
  "// main:": "//main:",
  "// a[0] também vira 99": "// a[0] also becomes 99",
  "// mesmo array": "// same array",
  "// [] -- clear() alterou o objeto real": "// [] -- clear() changed the actual object",
  "-- não precisa consultar banco/sessão nenhuma": "-- no need to consult any bank/session",
  "// muda o OBJETO apontado -> visível fora": "// changes the OBJECT pointed to -> visible outside",
  "// long somar(int a, int b) { return a + b; }": "// long add(int a, int b) { return a + b; }",
  "// 5 -- inalterado, primitivo copiado por valor": "// 5 -- unchanged, primitive copied by value",
  "// \"renomeado\" -- o objeto no heap foi alterado": "// \"renamed\" -- the object on the heap has changed",
  "// [Bruno] -- a reatribuição local não afetou \"nomes\" fora do método": "// [Bruno] -- local reassignment did not affect \"names\" outside the method",
  "// só troca a referência LOCAL -> invisível fora": "// only changes the LOCAL reference -> invisible outside",
  "1. Cliente envia login/senha\n2. Servidor valida e gera um JWT assinado, contendo: { \"sub\": \"usuario_42\", \"exp\": 1234567890 }\n3. Cliente guarda o token e manda em toda requisição: Authorization: Bearer eyJhbGc...\n4. Servidor apenas VERIFICA A ASSINATURA -- não precisa consultar banco/sessão nenhuma\n   para saber quem é o usuário (a menos que precise revogar o token antes da expiração)": "1. Customer sends login/password\n2. Server validates and generates a signed JWT, containing: { \"sub\": \"usuario_42\", \"exp\": 1234567890 }\n3. Client saves the token and sends every request: Authorization: Bearer eyJhbGc...\n4. Server just VERIFIES THE SIGNATURE -- no need to consult any bank/session\n   to know who the user is (unless you need to revoke the token before expiration)",
  "// ❌ NÃO compila: mesma assinatura, só o retorno muda": "// ❌ DOES NOT compile: same signature, only the return changes",
  "// ainda \"renomeado\" -- reatribuir o parâmetro local não afeta a variável externa": "// still \"renamed\" -- reassigning the local parameter does not affect the external variable",
  "// isso só quebra em RUNTIME, silenciosamente, como \"undefined\" na tela": "// this only breaks on RUNTIME, silently, as \"undefined\" on the screen",
  "// se a API mudar \"paginas\" para \"totalPaginas\" sem avisar,": "// if the API changes \"paginas\" to \"totalPaginas\" without warning,",
  "// desenvolvimento -- exatamente como o compilador Java (capítulo 00) faz": "// development -- exactly as the Java compiler (chapter 00) does",
  "// O(1) -- não importa o tamanho de \"lista\", sempre um passo:": "// O(1) -- no matter the size of the \"list\", always one step:",
  "// se a API mudar o campo, o editor avisa AGORA, em tempo de": "// if the API changes the field, the editor warns you NOW, in time to",
  "// se a API mudar o campo, o editor avisa AGORA, em tempo de\n  // desenvolvimento -- exatamente como o compilador Java (capítulo 00) faz": "// if the API changes the field, the editor warns you NOW, in time to\n  // development -- exactly as the Java compiler (chapter 00) does",
  "// O(n²) -- para cada elemento, percorre TODOS de novo -- CUIDADO:": "// O(n²) -- for each element, loop through ALL of them again -- CAUTION:",
  "// se a API mudar \"paginas\" para \"totalPaginas\" sem avisar,\n  // isso só quebra em RUNTIME, silenciosamente, como \"undefined\" na tela": "// if the API changes \"paginas\" to \"totalPaginas\" without warning,\n  // this only breaks on RUNTIME, silently, as \"undefined\" on the screen",
  "# roda os testes de src/test/java": "# run tests from src/test/java",
  "# compila o código em src/main/java": "# compile the code in src/main/java",
  "1. Cliente envia login/senha\n2. Servidor valida, cria uma sessão, guarda em memória/Redis: sessao_abc123 -> usuario_42\n3. Servidor devolve um cookie: Set-Cookie: JSESSIONID=abc123\n4. Toda requisição seguinte, o navegador manda o cookie automaticamente\n5. Servidor consulta \"abc123 -> usuario_42\" para saber quem está fazendo a requisição": "1. Customer sends login/password\n2. Server validates, creates a session, saves it in memory/Redis: sessao_abc123 -> user_42\n3. Server returns a cookie: Set-Cookie: JSESSIONID=abc123\n4. Every subsequent request, the browser automatically sends the cookie\n5. Server queries \"abc123 -> user_42\" to find out who is making the request",
  "// O(n) -- um passo POR elemento:": "// O(n) -- one step PER element:",
  "# apaga o diretório target/": "# delete target/ directory",
  "# gera um .jar (ou .war) em target/": "# generate a .jar (or .war) in target/",
  "# compila, testa e empacota": "# compile, test and package",
  "-- disponível em compilação, mas fornecida pelo ambiente em runtime -->": "-- available in compilation, but provided by the environment at runtime -->",
  "# executa a aplicação (com o plugin \"application\")": "# run the application (with the \"application\" plugin)",
  "-- padrão: disponível em compilação, teste e runtime -->": "-- default: available in compilation, test and runtime -->",
  "-- só disponível ao rodar testes (ex: JUnit, Mockito) -->": "-- only available when running tests (e.g. JUnit, Mockito) -->",
  "// build.gradle.kts (Kotlin DSL, padrão em projetos novos)": "// build.gradle.kts (Kotlin DSL, default in new projects)",
  "<!-- padrão: disponível em compilação, teste e runtime -->": "<!-- default: available in compilation, test and runtime -->",
  "<!-- disponível em compilação, mas fornecida pelo ambiente em runtime -->": "<!-- available in compilation, but provided by the environment at runtime -->",
  "<!-- só disponível ao rodar testes (ex: JUnit, Mockito) -->": "<!-- only available when running tests (e.g. JUnit, Mockito) -->",
  "# só roda os testes": "# only runs the tests",
  "// enum, não número mágico": "// enum, not magic number",
  "// b aponta para o MESMO objeto que a": "// b points to the SAME object as a",
  "# .github/workflows/ci.yml": "# .github/workflows/ci.yml",
  "// dias desde a última atualização?": "// days since last update?",
  "# só builda de verdade na branch principal": "# only actually build on the main branch",
  "# só roda se o job \"test\" passar antes": "# only runs if the \"test\" job passes first",
  "// \"latte\" -- a e b são a mesma referência!": "// \"latte\" -- a and b are the same reference!",
  "# Testcontainers precisa de Docker -- já vem pronto no runner": "# Testcontainers needs Docker -- it comes ready in the runner",
  "// desconto": "// discount",
  "# capítulo 57!": "#chapter 57!",
  "# baixa o código": "# download the code",
  "// Um expresso puro": "// A pure espresso",
  "// o que é \"1\"?": "// what is \"1\"?",
  "// validar": "// validate",
  "# docs/adr/0003-usar-mongodb-para-catalogo.md": "# docs/adr/0003-usar-mongodb-para-catalogo.md",
  "-- múltiplos clientes": "-- multiple clients",
  "# Status": "# Status",
  "# Decisão": "# Decision",
  "// calcular total": "// calculate total",
  "# Consequências": "# Consequences",
  "// notificar": "// notify",
  "# Contexto": "# Context",
  "// desconto adicional": "// additional discount",
  "// ❌ um método fazendo três coisas, precisando de comentários pra separar:": "// ❌ a method doing three things, requiring comments to separate:",
  "-- roupas têm tamanho/cor, eletrônicos têm voltagem/garantia.": "-- clothes have size/color, electronics have voltage/warranty.",
  "# ADR 0001: Usar JWT em vez de sessão tradicional": "# ADR 0001: Use JWT instead of traditional session",
  "// ✅ cada intenção é um método nomeado -- os comentários viram desnecessários:": "// ✅ each intent is a named method -- comments are unnecessary:",
  "-- escala horizontalmente": "--scale horizontally",
  "// segunda chamada é ignorada": "// second call is ignored",
  "// 0.0 -- não lança exceção": "// 0.0 -- does not throw exception",
  "// mantém ordem natural (alfabética)": "// maintains natural order (alphabetical)",
  "-- exige lista de revogação se necessário.": "-- require revocation list if necessary.",
  "// sobrescreve o valor anterior da mesma chave": "// overwrites the previous value of the same key",
  "// mantém ordem de inserção": "// maintain insertion order",
  "// insere na posição 0": "// insert at position 0",
  "// remove pelo valor": "// remove by value",
  "// for-each": "// for-each",
  "// modifica a lista durante a iteração": "// modify the list during iteration",
  "// ❌ lança ConcurrentModificationException:": "// ❌ throws ConcurrentModificationException:",
  "// ordenação \"natural\" -- só existe UMA": "// \"natural\" ordering -- there is only ONE",
  "// programe contra a interface, instancie a implementação": "// program against the interface, instantiate the implementation",
  "// Comparator: quantas ordenações alternativas você quiser, fora da classe": "// Comparator: as many alternative orderings as you want, outside the class",
  "// remove pelo índice -- CUIDADO com ambiguidade int vs Integer!": "// remove by index -- BEWARE of int vs Integer ambiguity!",
  "// ✅ correto -- Iterator sabe que a remoção está acontecendo:": "// ✅ correct -- Iterator knows the removal is happening:",
  "# sobe TODOS os serviços definidos": "# go up ALL defined services",
  "# derruba tudo E apaga os volumes (cuidado!)": "# take everything down AND erase the volumes (be careful!)",
  "# acompanha o log só do serviço \"api\"": "# monitors the log only for the \"api\" service",
  "// 2. SOMAR 1 (em um registrador da CPU)": "// 2. SUM 1 (in a CPU register)",
  "// 1. LER o valor atual da memória principal": "// 1. READ the current value from main memory",
  "# usa o Dockerfile da pasta atual": "# use the Dockerfile from the current folder",
  "# \"db\" -- o NOME do serviço vira hostname!": "# \"db\" -- the service NAME becomes hostname!",
  "// usa compareTo": "// use compareTo",
  "//db:5432/biblioteca": "//db:5432/library",
  "# docker-compose.yml": "# docker-compose.yml",
  "// \"cacheado\" antigo, nunca atualizado": "// old \"curly\", never updated",
  "// 3. GRAVAR o resultado de volta na memória": "// 3. WRITE the result back into memory",
  "// Thread B fica presa AQUI PARA SEMPRE,": "// Thread B gets stuck HERE FOREVER,",
  "// porque B pode estar lendo um valor": "// because B might be reading a value",
  "// mesmo depois de A marcar pronto=true --": "// even after marking ready=true --",
  "//db:5432/biblioteca  # \"db\" -- o NOME do serviço vira hostname!": "//db:5432/library # \"db\" -- the service NAME becomes hostname!",
  "# derruba tudo (mas mantém os volumes por padrão)": "# drops everything (but keeps volumes by default)",
  "// ... faz algo ...": "// ...do something...",
  "// Thread A chama isso": "// Thread A calls this",
  "// Thread B fica presa AQUI PARA SEMPRE,\n                                            // mesmo depois de A marcar pronto=true --\n                                            // porque B pode estar lendo um valor\n                                            // \"cacheado\" antigo, nunca atualizado\n                                            // da memória principal": "// Thread B gets stuck HERE FOREVER,\n                                            // even after marking ready=true --\n                                            // because B might be reading a value\n                                            // old \"curly\", never updated\n                                            // from main memory",
  "// SEM volatile": "// WITHOUT volatile",
  "-- o incremento de uma": "-- the increase of a",
  "// Thread 1:": "// Thread 1:",
  "// da memória principal": "// from main memory",
  "// espera conseguir travar B": "// hope to be able to stop B",
  "// Não significa \"ignorar cache\" literalmente e não torna valor++ atômico.": "// Does not mean \"bypass cache\" literally and does not make value++ atomic.",
  "// da mesma variável: isso fornece visibilidade e restrições de ordenação.": "// of the same variable: this provides visibility and ordering restrictions.",
  "// uma escrita volatile acontece-before de uma leitura posterior": "// a volatile write happens-before a later read",
  "// uma escrita volatile acontece-before de uma leitura posterior\n// da mesma variável: isso fornece visibilidade e restrições de ordenação.\n// Não significa \"ignorar cache\" literalmente e não torna valor++ atômico.": "// a volatile write happens-before a later read\n// of the same variable: this provides visibility and ordering restrictions.\n// Does not mean \"bypass cache\" literally and does not make value++ atomic.",
  "// tempo, Thread 1 fica esperando recursoB (que Thread 2 tem), e": "// time, Thread 1 waits for resourceB (which Thread 2 has), and",
  "// Thread 2 (rodando AO MESMO TEMPO):": "// Thread 2 (running AT THE SAME TIME):",
  "// espera conseguir travar A": "// hope to be able to stop A",
  "// se Thread 1 pegar recursoA e Thread 2 pegar recursoB QUASE ao mesmo\n// tempo, Thread 1 fica esperando recursoB (que Thread 2 tem), e\n// Thread 2 fica esperando recursoA (que Thread 1 tem) -- DEADLOCK,\n// as duas ficam bloqueadas PARA SEMPRE": "// if Thread 1 gets resourceA and Thread 2 gets resourceB ALMOST at the same time\n// time, Thread 1 waits for resourceB (which Thread 2 has), and\n// Thread 2 waits for resourceA (which Thread 1 has) -- DEADLOCK,\n// both are blocked FOREVER",
  "// se Thread 1 pegar recursoA e Thread 2 pegar recursoB QUASE ao mesmo": "// if Thread 1 gets resourceA and Thread 2 gets resourceB ALMOST at the same time",
  "// Thread 2 fica esperando recursoA (que Thread 1 tem) -- DEADLOCK,": "// Thread 2 waits for resourceA (which Thread 1 has) -- DEADLOCK,",
  "// as duas ficam bloqueadas PARA SEMPRE": "// both are blocked FOREVER",
  "// combinando DUAS operações assíncronas independentes:": "// combining TWO independent asynchronous operations:",
  "// roda em outra thread, não bloqueia aqui": "// runs in another thread, does not block here",
  "// as duas buscas rodam EM PARALELO, e \"combinado\" só resolve quando": "// the two searches run IN PARALLEL, and \"combined\" only resolves when",
  "// o código AQUI continua rodando IMEDIATAMENTE, sem esperar\n// o \"futuro\" terminar -- typical de programação assíncrona/reativa": "// the code HERE continues running IMMEDIATELY, without waiting\n// the \"future\" finish -- typical of asynchronous/reactive programming",
  "// o \"futuro\" terminar -- typical de programação assíncrona/reativa": "// the \"future\" finish -- typical of asynchronous/reactive programming",
  "// o código AQUI continua rodando IMEDIATAMENTE, sem esperar": "// the code HERE continues running IMMEDIATELY, without waiting",
  "// AMBAS terminarem -- muito mais rápido que buscar uma, esperar, depois": "// BOTH finish -- much faster than searching for one, waiting, then",
  "// sequer começar -- o cenário circular de espera se torna impossível": "// don't even start -- the circular waiting scenario becomes impossible",
  "// versão CORRIGIDA -- ambas as threads travam recursoA ANTES de recursoB, sempre:": "// FIXED version -- both threads lock resourceA BEFORE resourceB, always:",
  "// como as DUAS threads sempre pedem recursoA primeiro, uma delas": "// as BOTH threads always request resourceA first, one of them",
  "// como as DUAS threads sempre pedem recursoA primeiro, uma delas\n// sempre consegue pegar os dois locks em sequência antes da outra\n// sequer começar -- o cenário circular de espera se torna impossível": "// as BOTH threads always request resourceA first, one of them\n// always manages to get both locks in sequence before the other\n// don't even start -- the circular waiting scenario becomes impossible",
  "// sempre consegue pegar os dois locks em sequência antes da outra": "// always manages to get both locks in sequence before the other",
  "1. Cria uma nova requisição: POST http://localhost:8080/auth/login\n2. Aba \"Body\" → JSON: {\"email\": \"teste@teste.com\", \"senha\": \"123456\"}\n3. Envia e confere a resposta: {\"token\": \"eyJhbGc...\"}\n4. Copia o token, usa em requisições seguintes na aba \"Authorization\" → Bearer Token": "1. Create a new request: POST http://localhost:8080/auth/login\n2. \"Body\" tab → JSON: {\"email\": \"teste@teste.com\", \"password\": \"123456\"}\n3. Send and check the answer: {\"token\": \"eyJhbGc...\"}\n4. Copy the token and use it in subsequent requests in the \"Authorization\" tab → Bearer Token",
  "// buscar a outra sequencialmente": "// fetch the other sequentially",
  "// mais um passo encadeado": "// one more chained step",
  "// encadeia -- roda QUANDO o anterior terminar": "// chain -- run WHEN the previous one finishes",
  "// mesma ORDEM que tarefa1 -- nunca inverte!": "// same ORDER as task1 -- never reverse!",
  "// as duas buscas rodam EM PARALELO, e \"combinado\" só resolve quando\n// AMBAS terminarem -- muito mais rápido que buscar uma, esperar, depois\n// buscar a outra sequencialmente": "// the two searches run IN PARALLEL, and \"combined\" only resolves when\n// BOTH finish -- much faster than searching for one, waiting, then\n// fetch the other sequentially",
  "//localhost:8080/auth/login": "//localhost:8080/auth/login",
  "// desserializa o JSON, capítulo 25": "// deserialize JSON, chapter 25",
  "// enviando dados com autenticação:": "// sending data with authentication:",
  "// buscando a lista de livros:": "// searching for the list of books:",
  "// já vem parseado, sem precisar de .json()": "// is already parsed, without needing .json()",
  "// resposta.ok é false para 4xx/5xx, capítulo 26": "// answer.ok is false for 4xx/5xx, chapter 26",
  "// sem precisar repetir o header manualmente em cada chamada": "// without having to manually repeat the header on each call",
  "// interceptor: adiciona o token em TODA requisição automaticamente,": "// interceptor: adds the token to EVERY request automatically,",
  "// delega para o construtor de cima -- deve ser a 1ª linha": "// delegate to the top constructor -- must be the 1st line",
  "// interceptor: adiciona o token em TODA requisição automaticamente,\n// sem precisar repetir o header manualmente em cada chamada": "// interceptor: adds the token to EVERY request automatically,\n// without having to manually repeat the header on each call",
  "// new B(); imprime exatamente na ordem 1,2,3,4,5,6": "// new B(); prints exactly in the order 1,2,3,4,5,6",
  "// lista explícita!": "// explicit list!",
  "// o JWT do capítulo 52": "// the JWT of chapter 52",
  "// exemplo conceitual com Bucket4j (biblioteca de rate limiting):": "// conceptual example with Bucket4j (rate limiting library):",
  "// erro típico no console do navegador ao chamar a API sem CORS configurado:": "// typical error in the browser console when calling the API without CORS configured:",
  "//localhost:3000\", \"https://meufrontend.com\") // lista explícita!": "//localhost:3000\", \"https://myfrontend.com\") // explicit list!",
  "// \"Access to fetch at 'http://localhost:8080/livros' from origin": "// \"Access to fetch at 'http://localhost:8080/livros' from origin",
  "//  'http://localhost:3000' has been blocked by CORS policy\"": "// 'http://localhost:3000' has been blocked by CORS policy\"",
  "// parte do agregado, NUNCA acessado diretamente de fora": "// part of the aggregate, NEVER accessed directly from outside",
  "// erro típico no console do navegador ao chamar a API sem CORS configurado:\n// \"Access to fetch at 'http://localhost:8080/livros' from origin\n//  'http://localhost:3000' has been blocked by CORS policy\"": "// typical error in the browser console when calling the API without CORS configured:\n// \"Access to fetch at 'http://localhost:8080/livros' from origin\n// 'http://localhost:3000' has been blocked by CORS policy\"",
  "// 100 requisições/minuto": "// 100 requests/minute",
  "// necessário se usar cookies de autenticação": "// necessary if using authentication cookies",
  "// a regra de negócio VIVE aqui, não espalhada": "// the business rule LIVES here, not spread",
  "-- útil quando o bug pode estar    │": "-- useful when the bug may be │",
  "//localhost:5173\")": "//localhost:5173\")",
  "// Aggregate Root": "// Aggregate Root",
  "← clique aqui na margem para\n                                                       breakpoint de linha": "← click here in the margin to\n                                                       line breakpoint",
  "// ou, para um loop:": "// or, for a loop:",
  "# 1. conecta o repositório GitHub ao Railway (via interface web)\n# 2. Railway detecta o Dockerfile (capítulo 31) automaticamente\n# 3. configura as variáveis de ambiente no painel (capítulo 57):": "# 1. connect GitHub repository to Railway (via web interface)\n# 2. Railway detects Dockerfile (chapter 31) automatically\n# 3. configure the environment variables in the panel (chapter 57):",
  "# 2. Railway detecta o Dockerfile (capítulo 31) automaticamente": "# 2. Railway detects Dockerfile (chapter 31) automatically",
  "# 4. a cada push na branch configurada, Railway builda e faz deploy sozinho --\n# o CI/CD do capítulo 58 combinado com isso fecha o ciclo completo": "# 4. with each push to the configured branch, Railway builds and deploys it alone --\n# the CI/CD of chapter 58 combined with this closes the complete cycle",
  "← breakpoint condicional: \"livro.getPaginas() > 900\"\n                          -- só pausa quando encontrar um livro específico com\n                          mais de 900 páginas, ignorando todos os outros": "← conditional breakpoint: \"livro.getPaginas() > 900\"\n                          -- only pause when you find a specific book with\n                          over 900 pages, ignoring all others",
  "// clique com o botão direito no breakpoint já criado, e defina uma condição:": "// right click on the already created breakpoint, and define a condition:",
  "-- só pausa quando encontrar um livro específico com": "-- only pause when you find a specific book with",
  "// toda mudança passa por um método do Pedido -- nunca \"pedido.getItens().add(...)\" direto de fora": "// every change goes through a Request method -- never \"pedido.getItens().add(...)\" directly from outside",
  "# 1. conecta o repositório GitHub ao Railway (via interface web)": "# 1. connect GitHub repository to Railway (via web interface)",
  "//usuario@meu-servidor.com\"": "//usuario@my-server.com\"",
  "# 1. detecta o framework (Next.js, Vite, etc)": "#1. detects the framework (Next.js, Vite, etc)",
  "# 2. roda o build (npm run build)": "# 2. run the build (npm run build)",
  "# builda e sobe DIRETO no servidor remoto": "# build and upload DIRECTLY to the remote server",
  "# conecta o repositório, e a cada push:": "# connect the repository, and with each push:",
  "# na sua máquina local ou via CI/CD:": "# on your local machine or via CI/CD:",
  "# 4. a cada push na branch configurada, Railway builda e faz deploy sozinho --": "# 4. with each push to the configured branch, Railway builds and deploys it alone --",
  "# 3. publica os arquivos estáticos em uma CDN global automaticamente": "#3. publish the static files to a global CDN automatically",
  "# o CI/CD do capítulo 58 combinado com isso fecha o ciclo completo": "# the CI/CD of chapter 58 combined with this closes the complete cycle",
  "# 3. configura as variáveis de ambiente no painel (capítulo 57):": "# 3. configure the environment variables in the panel (chapter 57):",
  "# conecta o repositório, e a cada push:\n# 1. detecta o framework (Next.js, Vite, etc)\n# 2. roda o build (npm run build)\n# 3. publica os arquivos estáticos em uma CDN global automaticamente": "# connect the repository, and with each push:\n#1. detects the framework (Next.js, Vite, etc)\n# 2. run the build (npm run build)\n#3. publish the static files to a global CDN automatically",
  "# .env.production do front-end -- aponta para a API real, não localhost:": "# .env.production from the front end -- points to the real API, not localhost:",
  "# projeto Spring Boot (capítulos 43-59)": "# Spring Boot project (chapters 43-59)",
  "-- um container externo decide):": "-- an external container decides):",
  "# sobe os dois juntos, capítulo 32": "# go up together, chapter 32",
  "-- nunca vai para o build de produção -->": "-- never goes to production build -->",
  "<!-- nunca vai para o build de produção -->": "<!-- never goes to production build -->",
  "# estrutura típica de monorepo:": "# typical monorepo structure:",
  "# define os 3 serviços: api, front, db": "# defines the 3 services: api, front, db",
  "-- seu código decide tudo):": "-- your code decides everything):",
  "//api.biblioteca.com": "//api.biblioteca.com",
  "# local, no .gitignore": "# local, in .gitignore",
  "# projeto React/Vite": "# React/Vite project",
  "// roda DEPOIS de todas as dependências injetadas --": "// runs AFTER all injected dependencies --",
  "-- o lugar certo para inicialização que        │": "-- the right place to boot that │",
  "// libera o recurso antes da JVM desligar": "// release the resource before the JVM shuts down",
  "-- criando o objeto \"cru\", sem dependências│": "-- creating the \"raw\" object, without dependencies│",
  "// roda DEPOIS de todas as dependências injetadas --\n        // seguro para usar qualquer @Autowired daqui": "// runs AFTER all injected dependencies --\n        // safe to use any @Autowired from here",
  "// B precisa de A -- CICLO!": "// B needs A -- CYCLE!",
  "-- o Spring resolve o GRAFO       │": "-- Spring solves the GRAPH │",
  "-- o bean \"sabe\" seu próprio nome no        │": "-- the bean \"knows\" its own name in │",
  "// seguro para usar qualquer @Autowired daqui": "// safe to use any @Autowired from here",
  "-- o padrão) ou até ser descartado,      │": "-- the default) or until discarded, │",
  "// injeção manual": "// manual injection",
  "/* SMTP */": "/* SMTP */",
  "/* SMTP */ }": "/* SMTP */ }",
  "// A precisa de B": "// A needs B",
  "// qual instanciar primeiro, porque cada um exige que o outro já exista": "// which one to instantiate first, because each requires the other to already exist",
  "// application context form a cycle\" -- o Spring NÃO consegue decidir": "// application context form a cycle\" -- Spring CANNOT decide",
  "// depende da ABSTRAÇÃO (capítulo 16, o \"D\" de SOLID)": "// depends on ABSTRACTION (chapter 16, the \"D\" of SOLID)",
  "// erro na inicialização: \"The dependencies of some of the beans in the\n// application context form a cycle\" -- o Spring NÃO consegue decidir\n// qual instanciar primeiro, porque cada um exige que o outro já exista": "// error on initialization: \"The dependencies of some of the beans in the\n// application context form a cycle\" -- Spring CANNOT decide\n// which one to instantiate first, because each requires the other to already exist",
  "// injeção via CONSTRUTOR -- a dependência vem de FORA, PedidoServico não cria nada": "// injection via CONSTRUCTOR -- the dependency comes from OUTSIDE, PedidoServico does not create anything",
  "// erro na inicialização: \"The dependencies of some of the beans in the": "// error on initialization: \"The dependencies of some of the beans in the",
  "// ❌ PedidoServico CRIA sua própria dependência": "// ❌ PedidoServico CREATES its own dependency",
  "// quem \"monta\" a aplicação decide a implementação real:": "// whoever \"assembles\" the application decides the actual implementation:",
  "// --- classe de teste \"falsa\" (test double) ---": "// --- \"fake\" test class (test double) ---",
  "// \"montagem\" da aplicação, uma única vez:": "// \"assembly\" of the application, once:",
  "// injeção manual do \"mock\"": "// manual mock injection",
  "# baixe o Docker Desktop em docker.com (exige WSL2 habilitado)": "# download Docker Desktop from docker.com (requires WSL2 enabled)",
  "# Ubuntu/Debian -- use o repositório apt oficial documentado em docs.docker.com.": "# Ubuntu/Debian -- use the official apt repository documented at docs.docker.com.",
  "# Depois de instalar docker-ce e os plugins, valide:": "# After installing docker-ce and the plugins, validate:",
  "# Ubuntu/Debian -- use o repositório apt oficial documentado em docs.docker.com.\n# Evite executar scripts remotos diretamente com \"curl | sh\".\n# Depois de instalar docker-ce e os plugins, valide:": "# Ubuntu/Debian -- use the official apt repository documented at docs.docker.com.\n# Avoid running remote scripts directly with \"curl | sh\".\n# After installing docker-ce and the plugins, validate:",
  "# depois instale o Docker Desktop pelo site e reinicie": "# then install Docker Desktop from the website and restart",
  "# Evite executar scripts remotos diretamente com \"curl | sh\".": "# Avoid running remote scripts directly with \"curl | sh\".",
  "# abra o Docker Desktop uma vez para iniciar o daemon": "# open Docker Desktop once to start the daemon",
  "# baixa a imagem do Docker Hub": "# download the image from Docker Hub",
  "# se ainda não tiver o WSL2": "# if you don't already have WSL2",
  "# confirma que tudo está funcionando": "# confirm that everything is working",
  "# containers rodando": "# containers running",
  "# abre um terminal DENTRO do container": "# open a terminal INSIDE the container",
  "# -e: variável de ambiente": "# -e: environment variable",
  "# -d: roda em background (detached)": "# -d: runs in background (detached)",
  "# só aí os dados são realmente apagados": "# only then is the data actually deleted",
  "# ver a saída/log do container": "# view container output/log",
  "# \"dados_postgres\" é um volume nomeado, gerenciado pelo Docker --\n# mesmo removendo e recriando o container, os dados do banco sobrevivem": "# \"postgres_data\" is a named volume, managed by Docker --\n# even removing and recreating the container, the database data survives",
  "# remove o container (a imagem continua no disco)": "# remove the container (the image remains on disk)",
  "# -d: roda em background (detached)\n# -e: variável de ambiente\n# -p host:container: mapeia a porta 5432 da sua máquina para a 5432 do container": "# -d: runs in background (detached)\n# -e: environment variable\n# -p host:container: maps port 5432 on your machine to port 5432 on the container",
  "# -p host:container: mapeia a porta 5432 da sua máquina para a 5432 do container": "# -p host:container: maps port 5432 on your machine to port 5432 on the container",
  "// JOIN FETCH, capítulo 46 -- evita N+1 aqui!": "// JOIN FETCH, chapter 46 -- avoid N+1 here!",
  "// method reference, capítulo 13": "// method reference, chapter 13",
  "/*.jar app.jar": "/*.jar app.jar",
  "// no service:": "// no service:",
  "# --- ESTÁGIO 2: runtime (imagem final, enxuta, só com o necessário) ---": "# --- STAGE 2: runtime (final, lean image, with only what is necessary) ---",
  "# --- ESTÁGIO 1: build (usa o JDK completo, mas é DESCARTADO no final) ---": "# --- STAGE 1: build (uses the full JDK, but is DISPOSED at the end) ---",
  "# \"dados_postgres\" é um volume nomeado, gerenciado pelo Docker --": "# \"postgres_data\" is a named volume, managed by Docker --",
  "# constrói a imagem a partir do Dockerfile na pasta atual": "# build the image from the Dockerfile in the current folder",
  "# mesmo removendo e recriando o container, os dados do banco sobrevivem": "# even removing and recreating the container, the database data survives",
  "# lista imagens locais, compare o tamanho antes/depois do multi-stage": "# list local images, compare size before/after multi-stage",
  "# Dockerfile ingênuo -- funciona, mas é PROBLEMÁTICO:": "# Naive Dockerfile -- works, but is PROBLEMATIC:",
  "// achata o relacionamento -- o cliente da API nem precisa saber que existe uma tabela \"autores\"": "// flattens the relationship -- the API client doesn't even need to know that there is an \"authors\" table",
  "// gera um @Component pronto para injeção!": "// generates an @Component ready for injection!",
  "/* valida regra de negócio */ }": "/* validate business rule */ }",
  "// ainda permite saldo negativo!": "// still allows negative balance!",
  "// campo com nome diferente": "// field with different name",
  "/* valida regra de negócio */": "/* validate business rule */",
  "// MapStruct gera, em tempo de compilação, uma classe LivroMapperImpl": "// MapStruct generates, at compile time, a LivroMapperImpl class",
  "// MapStruct gera, em tempo de compilação, uma classe LivroMapperImpl\n// com exatamente o código que você escreveria manualmente -- sem custo\n// de reflection em runtime, ao contrário do Jackson (capítulo 25)": "// MapStruct generates, at compile time, a LivroMapperImpl class\n// with exactly the code you would write manually -- free of charge\n// of reflection at runtime, unlike Jackson (chapter 25)",
  "// \"modificar\" retorna um NOVO Ponto, nunca altera o atual": "// \"modify\" returns a NEW Point, never changes the current one",
  "// encapsulamento de verdade -- expõe operações, não o campo:": "// truth encapsulation -- exposes operations, not the field:",
  "// de reflection em runtime, ao contrário do Jackson (capítulo 25)": "// of reflection at runtime, unlike Jackson (chapter 25)",
  "// com exatamente o código que você escreveria manualmente -- sem custo": "// with exactly the code you would write manually -- at no cost",
  "// encapsulamento \"de fachada\" -- não resolve nada:": "// \"façade\" encapsulation -- doesn't solve anything:",
  "// Java não tem uma classe \"Stack\" idiomática -- usa-se Deque": "// Java doesn't have an idiomatic \"Stack\" class -- Deque is used",
  "// remove \"b\" -- o último que entrou": "// remove \"b\" -- the last one entered",
  "// remove \"a\" -- o primeiro que entrou": "// remove \"a\" -- the first one that entered",
  "// uso real: a própria stack de chamadas de função (capítulo 01, 81) é uma pilha!\n// e é assim que o método \"undo\" de um editor de texto costuma funcionar": "// actual usage: the function call stack itself (chapter 01, 81) is a stack!\n// and this is how a text editor's \"undo\" method usually works",
  "// e é assim que o método \"undo\" de um editor de texto costuma funcionar": "// and this is how a text editor's \"undo\" method usually works",
  "// uso real: a própria stack de chamadas de função (capítulo 01, 81) é uma pilha!": "// actual usage: the function call stack itself (chapter 01, 81) is a stack!",
  "// inserções ordenadas podem formar uma lista e degradar para O(n).": "// ordered insertions can form a list and degrade to O(n).",
  "// árvore de busca: esquerda menor, direita maior.": "// search tree: left smaller, right larger.",
  "// Busca é O(log n) quando a altura é logarítmica; sem balanceamento,": "// Search is O(log n) when height is logarithmic; no balancing,",
  "// árvore de busca: esquerda menor, direita maior.\n// Busca é O(log n) quando a altura é logarítmica; sem balanceamento,\n// inserções ordenadas podem formar uma lista e degradar para O(n).": "// search tree: left smaller, right larger.\n// Search is O(log n) when height is logarithmic; no balancing,\n// ordered insertions can form a list and degrade to O(n).",
  "// contrato: 123.45": "// contract: 123.45",
  "// caso base da recursão": "// base case of recursion",
  "// descarta a metade direita inteira": "// discard the entire right half",
  "// representação simples: lista de adjacência": "// simple representation: adjacency list",
  "// descarta a metade esquerda inteira": "// discard the entire left half",
  "↑ chave": "↑ key",
  "-- garantindo que,": "-- ensuring that,",
  "-- a aplicação cliente": "-- the client application",
  "// SEMPRE roda, com ou sem exceção": "// ALWAYS runs, with or without exception",
  "// tudo que abriu precisa ter fechado": "// everything that opened must have closed",
  "// problemas graves da JVM (OutOfMemoryError...) -- não trate": "// serious JVM problems (OutOfMemoryError...) -- do not address",
  "// CHECKED -- condição esperada (IOException, SQLException...)": "// CHECKED -- expected condition (IOException, SQLException...)",
  "// br.close() é chamado automaticamente aqui, sem precisar de finally": "// br.close() is called automatically here, without needing finally",
  "// UNCHECKED -- erro de programação (NullPointerException...)": "// UNCHECKED -- programming error (NullPointerException...)",
  "-- é assim que Kafka escala: paralelismo entre partições,": "-- this is how Kafka scales: parallelism between partitions,",
  "// busca em largura (BFS) -- percorre \"camada por camada\", usando uma Fila:": "// breadth-first search (BFS) -- traverses \"layer by layer\", using a Queue:",
  "// e.getCause() no catch externo recupera a SQLException original": "// e.getCause() in the outer catch recovers the original SQLException",
  "// \"e\" vira a \"cause\"": "// \"and\" becomes \"cause\"",
  "-- se ele cair": "-- if he falls",
  "// T = \"type parameter\", convenção de nome de uma letra": "// T = \"type parameter\", one-letter naming convention",
  "// T precisa ser algum tipo que implementa Comparable<T>": "// T needs to be some type that implements Comparable<T>",
  "// ClassCastException em RUNTIME, só descoberto ao rodar": "// ClassCastException in RUNTIME, only discovered when running",
  "// SEM generics (como era antes do Java 5): Object genérico, cast manual, perigoso": "// WITHOUT generics (as it was before Java 5): Generic Object, manual cast, dangerous",
  "// COM generics: o compilador barra o erro antes de rodar": "// COM generics: the compiler stops the error before running",
  "// renderize cada estado deliberadamente; não deixe erro parecer lista vazia": "// render each state deliberately; don't let error look like empty list",
  "// compila! nada impede misturar tipos": "// compile! nothing prevents mixing types",
  "// uso: String s = primeiro(listaDeStrings); Integer i = primeiro(listaDeInteiros);": "// usage: String s = first(listOfStrings); Integer i = first(listOfIntegers);",
  "// sem cast necessário!": "// no cast required!",
  "// lista2.add(42); // ❌ erro de COMPILAÇÃO -- muito mais barato que descobrir em produção": "// list2.add(42); // ❌ COMPILATION error -- much cheaper than finding out in production",
  "// ? extends Numero: aceita listas de Numero OU QUALQUER SUBTIPO -- só leitura segura": "// ? extends Number: accepts lists of Number OR ANY SUBTYPE -- safe read only",
  "# confirme a instalação e configure identidade (uma vez só, por máquina):": "# confirm the installation and configure identity (once only, per machine):",
  "# baixe o instalador em git-scm.com e siga o wizard, ou via winget:": "# download the installer from git-scm.com and follow the wizard, or via winget:",
  "// ? super Integer: aceita listas de Integer OU QUALQUER SUPERTIPO -- seguro para ESCRITA": "// ? super Integer: accepts lists of Integer OR ANY SUPERTYPE -- WRITE safe",
  "// funciona com List<Integer>, List<Double>, List<Number>...": "// works with List<Integer>, List<Double>, List<Number>...",
  "// uso: repo.buscar(livro -> livro.getPaginas() > 300);": "// usage: repo.buscar(book -> book.getPaginas() > 300);",
  "// uso: Par<String,Integer> p = new Par<>(\"idade\", 25); Par<Integer,String> inv = p.inverter();": "// usage: Par<String,Integer> p = new Par<>(\"age\", 25); Par<Integer,String> inv = p.inverter();",
  "# Fedora:": "# Fedora:",
  "# via Homebrew:": "# via Homebrew:",
  "# Debian/Ubuntu:": "# Debian/Ubuntu:",
  "# cria E muda, em um comando só": "# create AND change, in one command",
  "# o que mudou desde o último commit": "# what has changed since the last commit",
  "# adiciona TUDO que mudou": "# add EVERYTHING that changed",
  "# registra um snapshot permanente": "# records a permanent snapshot",
  "# cria um repositório novo na pasta atual": "# create a new repository in the current folder",
  "# cria uma branch nova": "# create a new branch",
  "# muda para ela": "# change to her",
  "# histórico resumido": "# summary history",
  "--- REQUISIÇÃO ---": "--- REQUEST ---",
  "--- RESPOSTA ---": "--- RESPONSE ---",
  "-- RESPOSTA ---": "-- RESPONSE ---",
  "# move para a \"staging area\" (o que vai entrar no próximo commit)": "# move to the \"staging area\" (what will be included in the next commit)",
  "# cria um commit de merge, preserva o histórico exato de cada branch": "# creates a merge commit, preserves the exact history of each branch",
  "# traz as mudanças de volta para main": "# bring changes back to main",
  "// new C().falar(); imprime \"A\", \"B\", \"C\" nessa ordem": "// new C().talk(); prints \"A\", \"B\", \"C\" in that order",
  "# cria SmsServico.java...": "# create SmsServico.java...",
  "# depois de terminar e commitar na branch:": "# after finishing and committing to the branch:",
  "// criar": "// to create",
  "// 1 -- considerados duplicados": "// 1 -- considered duplicates",
  "// JSON de resposta": "// response JSON",
  "// buscar um específico": "// search for a specific one",
  "-- REQUISIÇÃO ---": "-- REQUEST ---",
  "// remover": "// remove",
  "// listar todos": "// list all",
  "# reescreve os commits da sua branch como se tivessem partido do main atual -- histórico linear, mas reescreve commits": "# rewrites your branch's commits as if they came from the current main -- linear history, but rewrites commits",
  "// default: implementação padrão, subclasses podem sobrescrever ou não": "// default: default implementation, subclasses can override or not",
  "// também seria possível chamar Voador.super.mover() para reusar uma delas": "// it would also be possible to call Voador.super.mover() to reuse one of them",
  "// gera Main.class": "// generate Main.class",
  "// lambda -- capítulo 13": "// lambda -- chapter 13",
  "// substituir por completo": "// replace completely",
  "// ❌ estilo RPC, verbos na URL:": "// ❌ RPC style, verbs in URL:",
  "// ✅ estilo REST, recursos + métodos HTTP:": "// ✅ REST style, features + HTTP methods:",
  "//api.biblioteca.com/livros\"))": "//api.biblioteca.com/livros\"))",
  "// abstrato -- cada implementação define": "// abstract -- each implementation defines",
  "// obrigatório resolver o conflito manualmente": "// mandatory to resolve the conflict manually",
  "// 2. javac compila para bytecode:": "// 2. javac compiles to bytecode:",
  "// 1. Você escreve Main.java (código-fonte, texto legível)\n// 2. javac compila para bytecode:": "// 1. You write Main.java (source code, readable text)\n// 2. javac compiles to bytecode:",
  "// 3. a JVM interpreta/compila-JIT o bytecode e executa:": "// 3. the JVM interprets/JIT-compiles the bytecode and executes:",
  "// static: pertence à interface, chamado como Notificador.padrao()": "// static: belongs to the interface, called as Notificador.patrao()",
  "// 1. Você escreve Main.java (código-fonte, texto legível)": "// 1. You write Main.java (source code, readable text)",
  "// aguarda a tarefa terminar": "// wait for the task to finish",
  "// var só existe em variáveis LOCAIS -- nunca em campos, parâmetros ou retornos": "// var only exists in LOCAL variables -- never in fields, parameters or returns",
  "// gera automaticamente: construtor, getters (latitude(), longitude()), equals, hashCode, toString": "// automatically generates: constructor, getters (latitude(), longitude()), equals, hashCode, toString",
  "// o compilador infere: List<Livro>... na verdade ArrayList<Livro>": "// the compiler infers: List<Book>... actually ArrayList<Book>",
  "//       com um código de saída, não com um \"return\" de objeto).": "// with an exit code, not an object return).",
  "// ❌ o chamador precisa \"adivinhar\" que pode vir null:": "// ❌ the caller needs to \"guess\" which may come with null:",
  "// static: a JVM chama main SEM criar um objeto Main antes — precisa": "// static: the JVM calls main WITHOUT creating a Main object first — needs",
  "// public: a JVM (fora da classe) precisa conseguir chamar este método.\n// static: a JVM chama main SEM criar um objeto Main antes — precisa\n//         existir independente de qualquer instância.\n// void: main não devolve valor para quem o chamou (o processo termina\n//       com um código de saída, não com um \"return\" de objeto).": "// public: the JVM (outside the class) needs to be able to call this method.\n// static: the JVM calls main WITHOUT creating a Main object first — needs\n// exist independently of any instance.\n// void: main does not return a value to whoever called it (the process ends\n// with an exit code, not an object return).",
  "//         existir independente de qualquer instância.": "// exist independently of any instance.",
  "// public: a JVM (fora da classe) precisa conseguir chamar este método.": "// public: the JVM (outside the class) needs to be able to call this method.",
  "// void: main não devolve valor para quem o chamou (o processo termina": "// void: main does not return a value to whoever called it (the process ends",
  "// ✅ o TIPO já avisa: \"isso pode não existir\"": "// ✅ TIPO already warns: \"this may not exist\"",
  "// livro é inferido como Livro": "// book is inferred as Book",
  "// text block -- string multilinha, útil para JSON/SQL de exemplo": "// text block -- multiline string, useful for example JSON/SQL",
  "// ✅ PreparedStatement separa o valor da estrutura SQL": "// ✅ PreparedStatement separates the value from the SQL structure",
  "// findFirst() JÁ retorna Optional<T>": "// findFirst() ALREADY returns Optional<T>",
  "// preenche o \"?\" -- posição 1": "// fill in the \"?\" -- position 1",
  "// 2) chamar os setters correspondentes a cada campo do JSON": "// 2) call the setters corresponding to each JSON field",
  "// getters obrigatórios -- é assim que o Jackson DESCOBRE os campos a serializar": "// required getters -- this is how Jackson DISCOVERS the fields to serialize",
  "// Jackson usa reflection para: 1) criar o objeto via construtor sem args,": "// Jackson uses reflection to: 1) create the object via constructor without args,",
  "// INSERT/UPDATE/DELETE usam executeUpdate(), não executeQuery()": "// INSERT/UPDATE/DELETE use executeUpdate(), not executeQuery()",
  "// {\"titulo\":\"1984\",\"autor\":\"Orwell\",\"paginas\":328}": "// {\"title\":\"1984\",\"author\":\"Orwell\",\"pages\":328}",
  "// dirty checking: não precisa chamar save em entidade já gerenciada": "// dirty checking: no need to call save on an already managed entity",
  "// Jackson usa reflection para: 1) criar o objeto via construtor sem args,\n// 2) chamar os setters correspondentes a cada campo do JSON": "// Jackson uses reflection to: 1) create the object via constructor without args,\n// 2) call the setters corresponding to each JSON field",
  "// ❌ SQL Injection -- um usuário malicioso pode injetar \"'; DROP TABLE livros; --\"": "// ❌ SQL Injection -- a malicious user can inject \"'; DROP TABLE books; --\"",
  "// busca \"titulo\"│": "// search for \"title\"│",
  "// empilha \"this\" │": "// stack \"this\" │",
  "// retorna       │": "// returns │",
  "// sem precisar de getters \"get*\" (usa os métodos de acesso do record: titulo(), autor()...)": "// without needing \"get*\" getters (uses record access methods: title(), author()...)",
  "// Jackson serializa/desserializa records automaticamente desde a versão 2.12+ --": "// Jackson automatically serializes/deserializes records since version 2.12+ --",
  "// Jackson serializa/desserializa records automaticamente desde a versão 2.12+ --\n// sem precisar de getters \"get*\" (usa os métodos de acesso do record: titulo(), autor()...)": "// Jackson automatically serializes/deserializes records since version 2.12+ --\n// without needing \"get*\" getters (uses record access methods: title(), author()...)",
  "// {\"titulo\":\"O Alquimista\",\"autor\":\"Paulo Coelho\",\"paginas\":208}": "// {\"title\":\"The Alchemist\",\"author\":\"Paulo Coelho\",\"pages\":208}",
  "-- NÃO é código de máquina x86/ARM,": "-- NOT x86/ARM machine code,",
  "// renomeia o campo no JSON": "// rename the field in JSON",
  "//localhost:5432/biblioteca\";": "//localhost:5432/library\";",
  "// exception chaining (capítulo 10)": "// exception chaining (chapter 10)",
  "// nunca aparece no JSON de saída (ex: senha)": "// never appears in the output JSON (e.g. password)",
  "// objeto A ficou SEM NENHUMA referência --": "// object A was left WITHOUT ANY references --",
  "// objeto B criado, \"l1\" agora aponta para B": "// object B created, \"l1\" now points to B",
  "// objeto B criado, \"l1\" agora aponta para B\n                          // objeto A ficou SEM NENHUMA referência --\n                          // agora é candidato a coleta de lixo": "// object B created, \"l1\" now points to B\n                          // object A was left WITHOUT ANY references --\n                          // is now a candidate for garbage collection",
  "-- proteção de segurança), e registra": "-- security protection), and records",
  "// nunca remove nada!": "// never removes anything!",
  "// agora é candidato a coleta de lixo": "// is now a candidate for garbage collection",
  "// método termina -- o frame da stack é destruído --": "// method ends -- stack frame is destroyed --",
  "-- continue somente quando uma linha foi inserida": "-- continue only when a line has been inserted",
  "// objeto C, referenciado só pela variável local": "// C object, referenced only by the local variable",
  "// método termina -- o frame da stack é destruído --\n  // \"local\" deixa de existir -- objeto C também vira lixo": "// method ends -- stack frame is destroyed --\n  // \"local\" ceases to exist -- object C also becomes trash",
  "// \"local\" deixa de existir -- objeto C também vira lixo": "// \"local\" ceases to exist -- object C also becomes trash",
  "// objeto A criado no heap, \"l1\" (na stack) aponta para ele": "// object A created on the heap, \"l1\" (on stack) points to it",
  "# docker-compose.yml -- Kafka moderno não precisa mais de Zookeeper separado (modo KRaft):": "# docker-compose.yml -- Modern Kafka no longer needs separate Zookeeper (KRaft mode):",
  "// placeholder \"{}\", não concatenação": "// placeholder \"{}\", not concatenation",
  "// --- Producer: publica uma mensagem ---": "// --- Producer: publishes a message ---",
  "// --- Consumer: lê mensagens ---": "// --- Consumer: reads messages ---",
  "//:9092,CONTROLLER://:9093": "//:9092,CONTROLLER://:9093",
  "# dentro do container:": "# inside the container:",
  "//localhost:9092": "//localhost:9092",
  "# application.yml (formato hierárquico, também aceito pelo Spring Boot)": "# application.yml (hierarchical format, also supported by Spring Boot)",
  "// Decomposição em português ANTES do código:": "// Decomposition in Portuguese BEFORE the code:",
  "// Problema: \"dado um array, contar quantos números são positivos\"": "// Problem: \"given an array, count how many numbers are positive\"",
  "// exception vai no final -- imprime a stack trace": "// exception goes at the end -- prints the stack trace",
  "//localhost:5432/biblioteca": "//localhost:5432/library",
  "# application.properties (formato chave=valor)": "# application.properties (key=value format)",
  "# ${...} lê de variável de ambiente": "# ${...} reads from environment variable",
  "// 1. começar um contador em zero": "// 1. start a counter at zero",
  "// Problema: \"dado um array, contar quantos números são positivos\"\n//\n// Decomposição em português ANTES do código:\n// 1. começar um contador em zero\n// 2. percorrer cada número\n// 3. aumentar o contador quando o número for maior que zero\n// 4. devolver o contador\n//\n// SÓ AGORA traduzir para Java:": "// Problem: \"given an array, count how many numbers are positive\"\n//\n// Decomposition in Portuguese BEFORE the code:\n// 1. start a counter at zero\n// 2. cycle through each number\n// 3. increase the counter when the number is greater than zero\n// 4. return the counter\n//\n// JUST NOW translate to Java:",
  "// ... campos existentes ...": "// ... existing fields ...",
  "// 2. percorrer cada número": "// 2. cycle through each number",
  "// 3. aumentar o contador quando o número for maior que zero": "// 3. increase the counter when the number is greater than zero",
  "// retorna 24": "// returns 24",
  "// Decomposição:": "// Decomposition:",
  "// 4. devolver o contador": "// 4. return the counter",
  "// dry run manual de fatorial(4):\n// i=1: resultado = 1*1 = 1\n// i=2: resultado = 1*2 = 2\n// i=3: resultado = 2*3 = 6\n// i=4: resultado = 6*4 = 24\n// i=5: condição \"i<=4\" falsa, sai do loop\n// retorna 24": "// dry run manual of factorial(4):\n// i=1: result = 1*1 = 1\n// i=2: result = 1*2 = 2\n// i=3: result = 2*3 = 6\n// i=4: result = 6*4 = 24\n// i=5: condition \"i<=4\" false, exits the loop\n// returns 24",
  "// i=4: resultado = 6*4 = 24": "// i=4: result = 6*4 = 24",
  "// dry run manual de fatorial(4):": "// dry run manual of factorial(4):",
  "// SÓ AGORA traduzir para Java:": "// JUST NOW translate to Java:",
  "// i=1: resultado = 1*1 = 1": "// i=1: result = 1*1 = 1",
  "// i=5: condição \"i<=4\" falsa, sai do loop": "// i=5: condition \"i<=4\" false, exits the loop",
  "// Decomposição:\n// 1. guardar duas variáveis: \"maior\" e \"segundoMaior\"\n// 2. percorrer o array uma única vez\n// 3. se o valor atual for maior que \"maior\": o antigo \"maior\" vira \"segundoMaior\",\n//    e o valor atual vira o novo \"maior\"\n// 4. senão, se o valor atual for maior que \"segundoMaior\" (mas menor que \"maior\"):\n//    só atualiza \"segundoMaior\"": "// Decomposition:\n// 1. save two variables: \"largest\" and \"secondlargest\"\n// 2. traverse the array once\n// 3. if the current value is greater than \"largest\": the old \"largest\" becomes \"secondlargest\",\n// and the current value becomes the new \"largest\"\n// 4. else, if the current value is greater than \"largestsecond\" (but less than \"largest\"):\n// only updates \"largestsecond\"",
  "// i=3: resultado = 2*3 = 6": "// i=3: result = 2*3 = 6",
  "// i=2: resultado = 1*2 = 2": "// i=2: result = 1*2 = 2",
  "// 3. se o valor atual for maior que \"maior\": o antigo \"maior\" vira \"segundoMaior\",": "// 3. if the current value is greater than \"largest\": the old \"largest\" becomes \"secondlargest\",",
  "//    e o valor atual vira o novo \"maior\"": "// and the current value becomes the new \"largest\"",
  "// 4. senão, se o valor atual for maior que \"segundoMaior\" (mas menor que \"maior\"):": "// 4. else, if the current value is greater than \"largestsecond\" (but less than \"largest\"):",
  "//    só atualiza \"segundoMaior\"": "// only updates \"largestsecond\"",
  "// 1. guardar duas variáveis: \"maior\" e \"segundoMaior\"": "// 1. save two variables: \"largest\" and \"secondlargest\"",
  "/* ~10 linhas */": "/* ~10 lines */",
  "/* ~3 linhas */": "/* ~3 lines */",
  "/* ~3 linhas */ }": "/* ~3 lines */ }",
  "// 2. percorrer o array uma única vez": "// 2. traverse the array once",
  "// ~35 linhas para uma classe de 3 campos": "// ~35 lines for a 3-field class",
  "# application.properties": "# application.properties",
  "-- ignorando\");": "-- ignoring\");",
  "/* ~10 linhas */ }": "/* ~10 lines */ }",
  "-- V1__criar_tabela_autores.sql": "-- V1__criar_tabela_autores.sql",
  "// ✅ idempotente -- usa um ID único da mensagem para NUNCA processar duas vezes:": "// ✅ idempotent -- uses a unique message ID to NEVER process twice:",
  "// ❌ NÃO idempotente -- processar a mesma mensagem 2x credita 2x:": "// ❌ NOT idempotent -- processing the same message 2x credits 2x:",
  "// 8 linhas, mesmo resultado -- o código \"chato\" nunca existiu de verdade\n// no .java, mas existe no .class compilado final": "// 8 lines, same result -- the \"boring\" code never really existed\n// in .java, but exists in the final compiled .class",
  "-- V3__adicionar_coluna_isbn.sql (aplicada DEPOIS que V1 e V2 já rodaram)": "-- V3__add_coluna_isbn.sql (applied AFTER V1 and V2 have already run)",
  "// já vimos essa mensagem antes -- ignora silenciosamente": "// we've seen this message before -- silently ignore",
  "// 8 linhas, mesmo resultado -- o código \"chato\" nunca existiu de verdade": "// 8 lines, same result -- the \"boring\" code never really existed",
  "// no .java, mas existe no .class compilado final": "// in .java, but exists in the final compiled .class",
  "// setter customizado -- Lombok não gera validação, então mantém manual": "// custom setter -- Lombok does not generate validation, so keeps it manual",
  "// integra Mockito com o ciclo de vida JUnit 5 (capítulo 15)": "// integrate Mockito with the JUnit 5 lifecycle (chapter 15)",
  "// Mockito cria uma Biblioteca REAL, injetando os @Mock\n                    // declarados acima automaticamente no construtor": "// Mockito creates a REAL Library by injecting @Mock\n                    // declared above automatically in the constructor",
  "// NENHUMA classe NotificadorFalso manual, como no capítulo 22": "// NO class NotifierFalse manual, as in chapter 22",
  "# AUTOMATICAMENTE antes da aplicação subir, toda vez": "# AUTOMATICALLY before the application goes up, every time",
  "# o Spring Boot detecta o Flyway no classpath e roda as migrations\n# AUTOMATICAMENTE antes da aplicação subir, toda vez": "# Spring Boot detects Flyway in the classpath and runs migrations\n# AUTOMATICALLY before the application goes up, every time",
  "# o Spring Boot detecta o Flyway no classpath e roda as migrations": "# Spring Boot detects Flyway in the classpath and runs migrations",
  "// declarados acima automaticamente no construtor": "// declared above automatically in the constructor",
  "-- V3__adicionar_ano_publicacao.sql": "-- V3__add_year_publicacao.sql",
  "-- V2__criar_tabela_livros.sql": "-- V2__criar_tabela_livros.sql",
  "// CONFIRMA que o método FOI chamado": "// CONFIRM that the method WAS called",
  "// pelo menos 1 vez": "// at least 1 time",
  "// simplesmente devolveu o valor programado, instantaneamente": "// simply returned the programmed value, instantly",
  "// Mockito cria uma Biblioteca REAL, injetando os @Mock": "// Mockito creates a REAL Library by injecting @Mock",
  "// programando: \"QUANDO buscarPorId(1L) for chamado, RETORNE isso\"": "// programming: \"WHEN BuscaPorId(1L) is called, RETURN this\"",
  "// o servico NUNCA tocou em um banco de dados real -- repositorioMock": "// the service NEVER touched a real database -- repositorioMock",
  "// o servico NUNCA tocou em um banco de dados real -- repositorioMock\n    // simplesmente devolveu o valor programado, instantaneamente": "// the service NEVER touched a real database -- repositorioMock\n    // simply returned the programmed value, instantly",
  "// capturando o ARGUMENTO exato passado, para inspecionar seu conteúdo:": "// capturing the exact ARGUMENT passed, to inspect its contents:",
  "// NUNCA foi chamado": "// NEVER been called",
  "// Mockito CRIA um dublê dinâmico da interface -- sem você escrever": "// Mockito CREATES a dynamic interface double -- without you writing",
  "// Mockito CRIA um dublê dinâmico da interface -- sem você escrever\n              // NENHUMA classe NotificadorFalso manual, como no capítulo 22": "// Mockito CREATES a dynamic interface double -- without you writing\n              // NO class NotifierFalse manual, as in chapter 22",
  "// foi chamado exatamente 2 vezes": "// was called exactly 2 times",
  "// foi chamado, exatamente 1 vez (padrão)": "// was called, exactly 1 time (default)",
  "// equivalente a WHERE paginas > 300": "// equivalent to WHERE pages > 300",
  "// objeto aninhado, sem precisar de JOIN": "// nested object, no JOIN required",
  "// array direto no documento!": "// array directly in the document!",
  "// confere o CONTEÚDO da mensagem": "// check the CONTENT of the message",
  "// busca dentro do array": "// search within the array",
  "// EMBEDDING: dados aninhados dentro do mesmo documento": "// EMBEDDING: data nested within the same document",
  "// EMBEDDING: dados aninhados dentro do mesmo documento\n// bom quando os dados são sempre lidos juntos e não crescem sem limite": "// EMBEDDING: data nested within the same document\n// good when data is always read together and does not grow without limit",
  "# instalador oficial em mongodb.com/try/download/community": "# official installer at mongodb.com/try/download/community",
  "# abre o shell interativo do Mongo, similar ao psql": "# opens the Mongo interactive shell, similar to psql",
  "// REFERENCING: guarda só o ID, como uma chave estrangeira \"manual\"\n// bom quando os dados são grandes, mudam com frequência, ou são\n// compartilhados entre muitos documentos": "// REFERENCING: only stores the ID, like a \"manual\" foreign key\n// good when data is large, changes frequently, or is\n// shared among many documents",
  "// bom quando os dados são sempre lidos juntos e não crescem sem limite": "// good when data is always read together and does not grow without limit",
  "// o código parece completamente inocente:": "// the code looks completely innocent:",
  "// compartilhados entre muitos documentos": "// shared among many documents",
  "// entre 200 e 500": "// between 200 and 500",
  "// resultado: 1 (livros) + N (um SELECT de autor por livro) = N+1 queries!": "// result: 1 (books) + N (one SELECT author per book) = N+1 queries!",
  "// REFERENCING: guarda só o ID, como uma chave estrangeira \"manual\"": "// REFERENCING: only stores the ID, like a \"manual\" foreign key",
  "// o \"aggregate\" é o equivalente ao GROUP BY + JOIN combinados do SQL": "// \"aggregate\" is the equivalent of SQL's combined GROUP BY + JOIN",
  "// bom quando os dados são grandes, mudam com frequência, ou são": "// good when data is large, changes frequently, or is",
  "// PADRÃO do @ManyToOne se você não especificar!": "// @ManyToOne DEFAULT if you don't specify!",
  "// 1 query NOVA por livro para buscar o autor": "// 1 NEW query per book to search for the author",
  "// se @ManyToOne for EAGER (padrão!), dispara": "// if @ManyToOne is EAGER (default!), fire",
  "// se @ManyToOne for EAGER (padrão!), dispara\n                                                        // 1 query NOVA por livro para buscar o autor": "// if @ManyToOne is EAGER (default!), fire\n                                                        // 1 NEW query per book to search for the author",
  "// 1 query: SELECT * FROM livros": "// 1 query: SELECT * FROM books",
  "// opção 3: EntityGraph -- declarativo, mesmo efeito do JOIN FETCH": "// option 3: EntityGraph -- declarative, same effect as JOIN FETCH",
  "// findAll() + acessar .getAutor() em cada um = N+1": "// findAll() + access .getAutor() on each = N+1",
  "# application.properties -- mostra CADA query SQL executada:": "# application.properties -- shows EVERY SQL query executed:",
  "// versão com N+1 (usando findAll() padrão + acesso ao relacionamento):": "// version with N+1 (using standard findAll() + relationship access):",
  "// log mostra: 1 SELECT de livros + N SELECTs de autor (um por livro)": "// log shows: 1 book SELECT + N author SELECTs (one per book)",
  "// opção 1: LAZY por padrão (não resolve sozinho, só adia o problema)": "// option 1: LAZY by default (does not solve it alone, just postpones the problem)",
  "// opção 2: JOIN FETCH explícito -- resolve de verdade, tudo em UMA query": "// option 2: explicit JOIN FETCH -- actually resolves everything in ONE query",
  "// descarta metade esquerda": "// discard left half",
  "// false: referências distintas": "// false: distinct references",
  "// correção -- método dedicado no repository:": "// fix -- dedicated method in repository:",
  "// não encontrado": "// not found",
  "// true: mesmo conteúdo": "// true: same content",
  "// descarta metade direita": "// discard right half",
  "// evita overflow de (inicio+fim)/2 em arrays gigantes": "// avoid overflow of (start+end)/2 in giant arrays",
  "// O(log n) -- SÓ funciona se o array já estiver ORDENADO": "// O(log n) -- ONLY works if the array is already ORDERED",
  "// caso base -- 0 ou 1 elemento já está \"ordenado\"": "// base case -- 0 or 1 element is already \"sorted\"",
  "// log mostra UMA ÚNICA query, com JOIN, trazendo livro + autor juntos": "// log shows ONE SINGLE query, with JOIN, bringing book + author together",
  "// agora: List livros = livroRepository.buscarTodosComAutor();": "// now: List Livros = LivroRepository.buscarTodosComAutor();",
  "// agora: List livros = livroRepository.buscarTodosComAutor();\n// log mostra UMA ÚNICA query, com JOIN, trazendo livro + autor juntos": "// now: List Livros = LivroRepository.buscarTodosComAutor();\n// log shows ONE SINGLE query, with JOIN, bringing book + author together",
  "// configuração do Maven (capítulo 19)": "// Maven configuration (chapter 19)",
  "// combina os dois já ordenados": "// combines the two already ordered",
  "// import de tudo do pacote (evite em projetos grandes)": "// import everything from the package (avoid in large projects)",
  "// com/felipy/biblioteca/excecao/ItemIndisponivelException.java": "// com/felipy/biblioteca/excecao/ItemIndisponivelException.java",
  "// arquivo: src/main/java/com/felipy/biblioteca/modelo/Livro.java": "// file: src/main/java/com/felipy/biblioteca/modelo/Livro.java",
  "// import explícito": "// import explicit",
  "// mesma estrutura de pacotes, para os testes": "// same package structure for tests",
  "// com/felipy/biblioteca/modelo/Livro.java": "// com/felipy/biblioteca/modelo/Livro.java",
  "// com/felipy/biblioteca/servico/Biblioteca.java": "// com/felipy/biblioteca/servico/Biblioteca.java",
  "// com/felipy/biblioteca/modelo/ItemAcervo.java": "// com/felipy/biblioteca/modelo/ItemAcervo.java",
  "// implementação hoje: em memória. Amanhã: JDBC (capítulo 24). Depois: Spring Data JPA.": "// implementation today: in memory. Tomorrow: JDBC (chapter 24). After: Spring Data JPA.",
  "-- tudo": "-- all",
  "// notifica todos, sem saber quem são": "// notifies everyone, without knowing who they are",
  "// ... lógica de finalização ...": "// ... termination logic ...",
  "// 3000.0  (versão de Funcionario)\n// 4500.0  (versão sobrescrita em Gerente)": "// 3000.0 (Employee version)\n// 4500.0 (overwritten version in Manager)",
  "// 4500.0  (versão sobrescrita em Gerente)": "// 4500.0 (overwritten version in Manager)",
  "// 3000.0  (versão de Funcionario)": "// 3000.0 (Employee version)",
  "<!-- defina springdoc.version no pom conforme a matriz de compatibilidade\n     da sua versão do Spring Boot; acesse /swagger-ui.html -->": "<!-- define springdoc.version in the pom according to the compatibility matrix\n     your version of Spring Boot; go to /swagger-ui.html -->",
  "// implementação hoje: em memória. Amanhã: JDBC (capítulo 24). Depois: Spring Data JPA.\n// o código que USA LivroRepositorio não muda em NENHUM desses casos.": "// implementation today: in memory. Tomorrow: JDBC (chapter 24). After: Spring Data JPA.\n// the code that USES LivroRepositorio does not change in ANY of these cases.",
  "// construtor PRIVADO -- ninguém de fora cria uma nova": "// PRIVATE constructor -- no one from outside creates a new one",
  "// o código que USA LivroRepositorio não muda em NENHUM desses casos.": "// the code that USES LivroRepositorio does not change in ANY of these cases.",
  "// o \"cliente\" escolhe a estratégia em runtime, sem if/else espalhado": "// the \"client\" chooses the strategy at runtime, without if/else spread",
  "// GET /livros?page=0&size=20&sort=titulo,desc&titulo=duna": "// GET /livros?page=0&size=20&sort=title,desc&title=dune",
  "// forma clássica": "// classic form",
  "// downcast explícito": "// explicit downcast",
  "// instância -- binding DINÂMICO": "// instance -- DYNAMIC binding",
  "// static -- binding ESTÁTICO": "// static -- STATIC binding",
  "// atributo -- binding ESTÁTICO": "// attribute -- STATIC binding",
  "// \"Au au\" -- método pelo tipo do OBJETO": "// \"Woof woof\" -- method by OBJECT type",
  "// \"c\" já vem com o cast feito": "// \"c\" already has the cast made",
  "-- defina springdoc.version no pom conforme a matriz de compatibilidade": "-- define springdoc.version in the pom according to the compatibility matrix",
  "# já vimos isso no capítulo 30 -- é assim que praticamente todo mundo\n# roda Postgres em desenvolvimento hoje em dia:": "# we already saw this in chapter 30 -- this is how practically everyone\n# runs Postgres in development nowadays:",
  "// \"reino Animalia\" -- static pelo tipo da VARIÁVEL (evite chamar static assim!)": "// \"Animalia kingdom\" -- static by the type of the VARIABLE (avoid calling static like that!)",
  "// pattern matching for instanceof (Java 16+) -- mais direto": "// pattern matching for instanceof (Java 16+) -- more direct",
  "// \"animal genérico\" -- atributo lido pelo tipo da VARIÁVEL": "// \"generic animal\" -- attribute read by the VARIABLE type",
  "# roda Postgres em desenvolvimento hoje em dia:": "# runs Postgres in development nowadays:",
  "-- sair": "-- to go out",
  "-- listar bancos": "-- list banks",
  "# ou via winget:": "# or via winget:",
  "-- descrever a estrutura da tabela livros": "-- describe the structure of the books table",
  "-- sem índice em autor_id:": "-- no index on author_id:",
  "# já vimos isso no capítulo 30 -- é assim que praticamente todo mundo": "# we already saw this in chapter 30 -- this is how practically everyone",
  "# instalador oficial em postgresql.org/download/windows": "# official installer at postgresql.org/download/windows",
  "# instalador oficial em postgresql.org/download/windows\n# ou via winget:": "# official installer at postgresql.org/download/windows\n# or via winget:",
  "\\l          -- listar bancos\n\\c biblioteca  -- conectar a um banco\n\\dt         -- listar tabelas\n\\d livros   -- descrever a estrutura da tabela livros\n\\q          -- sair": "\\l -- list banks\n\\c library -- connect to a database\n\\dt -- list tables\n\\d books -- describe the structure of the books table\n\\q -- exit",
  "-- depois de \"CREATE INDEX idx_livros_autor ON livros(autor_id);\":": "-- after \"CREATE INDEX idx_livros_autor ON Livros(autor_id);\":",
  "// exatamente o mesmo padrão do capítulo 24 -- agora com um Postgres real rodando": "// exactly the same pattern as in chapter 24 -- now with real Postgres running",
  "# cliente de linha de comando -- funciona igual, instalado localmente ou via Docker:": "# command line client -- works the same, installed locally or via Docker:",
  "-- conectar a um banco": "-- connect to a bank",
  "-- listar tabelas": "-- list tables",
  "-- provavelmente Seq Scan, já que a tabela é pequena o suficiente para o": "-- probably Seq Scan, since the table is small enough for the",
  "-- com uma tabela pequena, o Postgres pode CONTINUAR escolhendo Seq Scan --\n-- e essa é a lição real: índice não é garantia automática de velocidade,\n-- o \"planner\" decide com base em estatísticas reais da tabela. O efeito\n-- fica claro em tabelas de centenas de milhares de linhas ou mais.": "-- with a small table, Postgres can KEEP choosing Seq Scan --\n-- and that's the real lesson: index is not an automatic guarantee of speed,\n-- the \"planner\" decides based on real statistics from the table. The effect\n-- is clear in tables of hundreds of thousands of rows or more.",
  "-- provavelmente Seq Scan, já que a tabela é pequena o suficiente para o\n-- planner decidir que vale mais a pena ler tudo do que usar índice": "-- probably Seq Scan, since the table is small enough for the\n-- planner decides that it's more worth reading everything than using an index",
  "// comportamento padrão, sem risco": "// default behavior, no risk",
  "// lógica nova, só ativa com a flag": "// new logic, only active with the flag",
  "-- planner decidir que vale mais a pena ler tudo do que usar índice": "-- planner decides that it's more worth reading everything than using an index",
  "-- fica claro em tabelas de centenas de milhares de linhas ou mais.": "-- is clear in tables of hundreds of thousands of rows or more.",
  "-- versão atual, clientes antigos continuam funcionando": "-- current version, old clients continue to work",
  "-- com uma tabela pequena, o Postgres pode CONTINUAR escolhendo Seq Scan --": "-- with a small table, Postgres can KEEP choosing Seq Scan --",
  "-- e essa é a lição real: índice não é garantia automática de velocidade,": "-- and that's the real lesson: index is not an automatic guarantee of speed,",
  "-- nova versão, com mudanças incompatíveis, coexistindo com v1": "-- new version, with incompatible changes, coexisting with v1",
  "-- o \"planner\" decide com base em estatísticas reais da tabela. O efeito": "-- the \"planner\" decides based on real statistics from the table. The effect",
  "// comportamento padrão, sempre seguro": "// default behavior, always safe",
  "# compila e cria Main.class": "# compile and create Main.class",
  "# pede à JVM para executar Main": "# ask the JVM to execute Main",
  "// --- teste JUnit ---": "// --- JUnit test ---",
  "// 1ª passada: cria todas as instâncias (construtor sem argumentos)": "// 1st pass: creates all instances (constructor without arguments)",
  "// 2ª passada: injeta dependências em cada campo @Injetar": "// 2nd pass: inject dependencies into each field @Inject",
  "# execução direta de arquivo-fonte em Java moderno": "# direct execution of source file in modern Java",
  "/* ... usa notificador normalmente ... */ }": "/* ... uses notifier normally ... */ }",
  "// aceita interface OU implementação exata": "// accept interface OR exact implementation",
  "// CASO BASE -- sem isso, recursão infinita": "// BASE CASE -- without this, infinite recursion",
  "/* ... usa notificador normalmente ... */": "/* ... use notifier normally ... */",
  "// --- usando o mini-container ---": "// --- using mini-container ---",
  "// preenchido automaticamente pelo container!": "// automatically populated by the container!",
  "//             = 4 * (3 * (2 * fatorial(1)))": "// = 4 * (3 * (2 * factorial(1)))",
  "// fatorial(4) = 4 * fatorial(3)\n//             = 4 * (3 * fatorial(2))\n//             = 4 * (3 * (2 * fatorial(1)))\n//             = 4 * (3 * (2 * 1))\n//             = 24": "// factorial(4) = 4 * factorial(3)\n// = 4 * (3 * factorial(2))\n// = 4 * (3 * (2 * factorial(1)))\n// = 4 * (3 * (2 * 1))\n// = 24",
  "// fatorial(4) = 4 * fatorial(3)": "// factorial(4) = 4 * factorial(3)",
  "// já calculado, não refaz": "// already calculated, do not redo",
  "//             = 4 * (3 * fatorial(2))": "// = 4 * (3 * factorial(2))",
  "// biblioteca já está pronta, com \"notificador\" injetado -- ZERO \"new\" manual!": "// library is ready, with \"notifier\" injected -- ZERO \"new\" manual!",
  "// ✅ com memoization -- O(n), guarda resultados já calculados:": "// ✅ with memoization -- O(n), saves already calculated results:",
  "// mesmo resultado, SEM o custo de empilhar N frames na stack": "// same result, WITHOUT the cost of stacking N frames on the stack",
  "// ❌ ingênua -- O(2ⁿ), recalcula os mesmos valores repetidamente:": "// ❌ naive -- O(2ⁿ), recalculates the same values ​​repeatedly:",
  "// versão iterativa do mesmo fatorial (capítulo 79, raciocínio de loop):": "// iterative version of the same factorial (chapter 79, loop reasoning):",
  "// CASO RECURSIVO -- problema menor + chamada a si mesmo": "// RECURSIVE CASE -- minor problem + call itself",
  "// caso base: número de um único dígito": "// base case: single digit number",
  "// somaDigitos(1234) = 4 + somaDigitos(123)": "// sumDigitos(1234) = 4 + sumDigitos(123)",
  "// último dígito + soma do resto": "// last digit + sum of remainder",
  "# quanto tempo falta para expirar": "# how long until it expires",
  "# cria a chave já com TTL atômico": "# create the key with atomic TTL",
  "# set -- sem duplicatas, como o capítulo 11": "# set -- no duplicates, like chapter 11",
  "# lista -- fila simples": "# list -- simple queue",
  "# shell interativo": "# interactive shell",
  "# string simples": "# simple string",
  "# operação atômica -- lembra da race condition do capítulo 14!": "# atomic operation -- remember the race condition from chapter 14!",
  "//                    = 4 + (3 + somaDigitos(12))": "// = 4 + (3 + sumDigits(12))",
  "// somaDigitos(1234) = 4 + somaDigitos(123)\n//                    = 4 + (3 + somaDigitos(12))\n//                    = 4 + (3 + (2 + somaDigitos(1)))\n//                    = 4 + 3 + 2 + 1 = 10": "// sumDigitos(1234) = 4 + sumDigitos(123)\n// = 4 + (3 + sumDigits(12))\n// = 4 + (3 + (2 + sumDigitos(1)))\n// = 4 + 3 + 2 + 1 = 10",
  "//                    = 4 + (3 + (2 + somaDigitos(1)))": "// = 4 + (3 + (2 + sumDigitos(1)))",
  "// guarda para a PRÓXIMA vez": "// save it for NEXT time",
  "// pode falhar, pode demorar": "// may fail, may take time",
  "// achou no cache -- retorna sem tocar no banco": "// found in cache -- returns without touching the bank",
  "// pode lançar exceção ou demorar demais": "// may throw exception or take too long",
  "// cliente Redis (biblioteca Jedis)": "// Redis client (Jedis library)",
  "// não achou -- vai no Postgres": "// didn't find it -- go to Postgres",
  "# hash -- como um objeto/Map dentro da chave": "# hash -- as an object/Map inside the key",
  "# 2. remove do controle de versão SEM apagar o arquivo local": "#2. remove from version control WITHOUT deleting the local file",
  "// chamado automaticamente quando o circuito está \"aberto\" (desarmado)": "// automatically called when the circuit is \"open\" (disarmed)",
  "# o arquivo continua no histórico antigo do Git, então a credencial": "# the file remains in the old Git history, so the credential",
  "# antiga precisa ser considerada COMPROMETIDA e substituída": "# old needs to be considered COMPROMISED and replaced",
  "// valor padrão -- melhor que travar o checkout inteiro": "// default value -- better than crashing the entire checkout",
  "# .gitignore -- capítulo 29, regra de ouro reforçada:": "# .gitignore -- chapter 29, reinforced rule of thumb:",
  "-- indo ao banco\");": "-- going to the bank\");",
  "# 1. adiciona ao .gitignore para não commitar de novo": "# 1. add to .gitignore to not commit again",
  "# 3. MAIS IMPORTANTE: troca a senha/chave real no provedor --": "# 3. MOST IMPORTANT: change the real password/key at the provider --",
  "# 3. MAIS IMPORTANTE: troca a senha/chave real no provedor --\n# o arquivo continua no histórico antigo do Git, então a credencial\n# antiga precisa ser considerada COMPROMETIDA e substituída": "# 3. MOST IMPORTANT: change the real password/key at the provider --\n# the file remains in the old Git history, so the credential\n# old needs to be considered COMPROMISED and replaced",
  "// 300s = 5 minutos": "// 300s = 5 minutes",
  "# .env (arquivo LOCAL, nunca commitado)": "# .env (LOCAL file, never committed)",
  "/**\").hasAuthority(\"SCOPE_pedidos:write\")": "/**\").hasAuthority(\"SCOPE_pedidos:write\")",
  "// somente para API Bearer stateless; não copie para autenticação por cookie": "// only for stateless Bearer API; do not copy for cookie authentication",
  "// ✅ responsabilidades separadas:": "// ✅ separate responsibilities:",
  "-- publica \"PedidoCriado\"": "-- publishes \"RequestCreated\"",
  "-- publica \"PagamentoAprovado\"": "-- publishes \"PaymentApproved\"",
  "// ❌ uma classe fazendo três coisas:": "// ❌ a class doing three things:",
  "-- publica \"EstoqueReservado\"": "-- publishes \"EstoqueReservado\"",
  "1. Serviço de Pedido: cria pedido (status PENDENTE) -- publica \"PedidoCriado\"\n2. Serviço de Estoque: reserva o item -- publica \"EstoqueReservado\"\n   (se falhar: publica \"EstoqueIndisponivel\" -> Pedido é CANCELADO, sem próximo passo)\n3. Serviço de Pagamento: cobra o cliente -- publica \"PagamentoAprovado\"\n   (se falhar: publica \"PagamentoRecusado\" -> COMPENSAÇÃO: Estoque é liberado de volta)\n4. Serviço de Pedido: marca pedido como CONFIRMADO": "1. Order Service: creates order (PENDING status) -- publishes \"OrderCreated\"\n2. Stock Service: reserves the item -- publishes \"EstoqueReservado\"\n   (if it fails: publishes \"EstoqueIndisponivel\" -> Order is CANCELED, with no next step)\n3. Payment Service: charges the customer -- publishes \"PaymentApproved\"\n   (if failed: publishes \"Payment Refused\" -> COMPENSATION: Stock is released back)\n4. Order Service: marks order as CONFIRMED",
  "// ✅ Forma.area() já resolve isso -- adicionar uma forma nova não toca em código existente": "// ✅ Forma.area() already solves this -- adding a new shape does not touch existing code",
  "// ❌ Quadrado \"é um\" retângulo geometricamente, mas quebra o CONTRATO:": "// ❌ Square \"is a\" rectangle geometrically, but it breaks the CONTRACT:",
  "// ❌ toda vez que aparece uma forma nova, esse método precisa mudar:": "// ❌ every time a new shape appears, this method needs to change:",
  "// ❌ acoplado à implementação concreta:": "// ❌ coupled to concrete implementation:",
  "// efeito colateral inesperado!": "// unexpected side effect!",
  "// ❌ interface inchada:": "// ❌ bloated interface:",
  "// travado nessa implementação": "// stuck on this implementation",
  "// ✅ interfaces segregadas:": "// ✅ segregated interfaces:",
  "// injeção de dependência": "// dependency injection",
  "/* robô não come! método forçado e sem sentido */": "/* robot doesn't eat! forced and meaningless method */",
  "/* robô não come! método forçado e sem sentido */ }": "/* robot doesn't eat! forced and meaningless method */ }",
  "// ... um \"else if\" novo para cada forma futura": "// ... a new \"else if\" for each future form",
  "// Carro TEM UM Motor -- composição": "// Car HAS AN Engine -- composition",
  "// ✅ depende da abstração, implementação é injetada de fora:": "// ✅ depends on abstraction, implementation is injected from outside:",
  "// intercepta qualquer método marcado com @MedirTempo": "// intercept any method marked with @MedirTempo",
  "// \"pointcut\": QUAIS métodos interceptar -- todo método em qualquer classe do pacote servico": "// \"pointcut\": WHICH methods to intercept -- every method in any class in the service package",
  "// em vez de \"Carro extends Motor\" (Carro NÃO é um tipo de Motor):": "// instead of \"Car extends Engine\" (Car is NOT a type of Engine):",
  "// chama o método REAL -- exatamente como metodo.invoke() do capítulo 20": "// call the REAL method -- exactly like metodo.invoke() from chapter 20",
  "// código que confia que setLargura só muda a largura QUEBRA silenciosamente com Quadrado": "// code that trusts that setWidth only changes the width BREAKS silently with Square",
  "/* SMTP direto aqui */ }": "/* Direct SMTP here */ }",
  "/* SQL direto aqui */ }": "/* Direct SQL here */ }",
  "/* SMTP direto aqui */": "/* Direct SMTP here */",
  "/* SQL direto aqui */": "/* Direct SQL here */",
  "// uso, em qualquer service:": "// use, in any service:",
  "// mesma lógica do exercício 38.1, automática": "// same logic as exercise 38.1, automatic",
  "// #result referencia o retorno do método": "// #result references the method return",
  "// ... atualiza no banco ...": "// ... updates in the database ...",
  "-- só aparece no CACHE MISS\");": "-- only appears in CACHE MISS\");",
  "// genérico -- \"isso é um bean gerenciado pelo Spring\"": "// generic -- \"this is a Spring-managed bean\"",
  "# application.properties -- conectando ao Redis real (capítulo 38):": "# application.properties -- connecting to real Redis (chapter 38):",
  "// especialização -- comunica INTENÇÃO: \"isso é lógica de negócio\"": "// specialization -- communicates INTENT: \"this is business logic\"",
  "// invalida o cache quando o dado muda -- o problema do capítulo 38!": "// invalidate the cache when the data changes -- the problem from chapter 38!",
  "// não registre args: podem conter senha, token ou PII": "// do not log args: may contain password, token or PII",
  "// SEMPRE relance -- um aspecto nunca deve engolir a exceção original": "// ALWAYS glance -- an aspect should never swallow the original exception",
  "// injeção via construtor -- a MELHOR forma (capítulo 22)": "// injection via constructor -- the BEST way (chapter 22)",
  "// seu capítulo 28:              →  Spring real:": "// your chapter 28: → Spring real:",
  "// MiniContainer.registrar()     →  @ComponentScan (automático, via reflection)": "// MiniContainer.registrar() → @ComponentScan (automatic, via reflection)",
  "// PADRÃO -- uma única instância para toda a aplicação (capítulo 23!)": "// DEFAULT -- a single instance for the entire application (chapter 23!)",
  "// uma instância NOVA a cada vez que é injetada/solicitada": "// a NEW instance each time it is injected/requested",
  "// capítulo 25 -- Jackson não é sua classe, não pode ter @Component": "// chapter 25 -- Jackson is not your class, it cannot have @Component",
  "// @Componente                   →  @Component (ou especializações abaixo)": "// @Component → @Component (or specializations below)",
  "// MiniContainer.resolver()      →  context.getBean(Tipo.class)": "// MiniContainer.resolver() → context.getBean(Type.class)",
  "// @Autowired implícito": "// @Autowired implicit",
  "// vira: WHERE autor.nome = ?": "// becomes: WHERE author.name = ?",
  "// equivale a SERIAL do capítulo 34": "// equivalent to SERIAL from chapter 34",
  "// @Injetar                      →  @Autowired": "// @Inject → @Autowired",
  "// vira: WHERE paginas > ?": "// turn: WHERE pages > ?",
  "// a chave estrangeira do capítulo 33": "// the foreign key of chapter 33",
  "// seu capítulo 28:              →  Spring real:\n// @Componente                   →  @Component (ou especializações abaixo)\n// @Injetar                      →  @Autowired\n// MiniContainer.registrar()     →  @ComponentScan (automático, via reflection)\n// MiniContainer.resolver()      →  context.getBean(Tipo.class)": "// your chapter 28: → Spring real:\n// @Component → @Component (or specializations below)\n// @Inject → @Autowired\n// MiniContainer.registrar() → @ComponentScan (automatic, via reflection)\n// MiniContainer.resolver() → context.getBean(Type.class)",
  "// vira: WHERE titulo = ? (retorna Optional!)": "// turns: WHERE title = ? (returns Optional!)",
  "// getters e setters...": "// getters and setters...",
  "// SELECT * FROM livros": "// SELECT * FROM books",
  "// INSERT ou UPDATE": "// INSERT or UPDATE",
  "// isso é TUDO. O Spring Data JPA gera a implementação inteira em runtime,": "// that's ALL. Spring Data JPA generates the entire implementation at runtime,",
  "// construtor sem args OBRIGATÓRIO (Hibernate cria via reflection, capítulo 20)": "// constructor without args REQUIRED (Hibernate creates via reflection, chapter 20)",
  "// construtor sem args OBRIGATÓRIO (Hibernate cria via reflection, capítulo 20)\n    // getters e setters...": "// constructor without args REQUIRED (Hibernate creates via reflection, chapter 20)\n    // getters and setters...",
  "// isso é TUDO. O Spring Data JPA gera a implementação inteira em runtime,\n    // via um Dynamic Proxy (capítulo 20!) que traduz cada chamada em SQL.": "// that's ALL. Spring Data JPA generates the entire implementation at runtime,\n    // via a Dynamic Proxy (chapter 20!) that translates each call into SQL.",
  "// via um Dynamic Proxy (capítulo 20!) que traduz cada chamada em SQL.": "// via a Dynamic Proxy (chapter 20!) that translates each call into SQL.",
  "// DELETE ... WHERE id = ?": "// DELETE ... WHERE id = ?",
  "// já vem de graça, sem você escrever nada:": "// it comes for free, without you writing anything:",
  "// SELECT ... WHERE id = ?  -- Optional<Livro>, capítulo 21": "// SELECT ... WHERE id = ?  -- Optional<Book>, chapter 21",
  "// @Controller + @ResponseBody: retorno vira JSON automaticamente (via Jackson, capítulo 25)": "// @Controller + @ResponseBody: return automatically becomes JSON (via Jackson, chapter 25)",
  "// producer: uma linha, o Spring já gerencia a configuração via application.properties": "// producer: one line, Spring already manages the configuration via application.properties",
  "// consumer: uma anotação substitui o loop de poll() inteiro": "// consumer: an annotation replaces the entire poll() loop",
  "# application.properties (compartilhado por todos os profiles)": "# application.properties (shared by all profiles)",
  "# application-dev.properties (só quando o profile \"dev\" está ativo)": "# application-dev.properties (only when the \"dev\" profile is active)",
  "# application.properties (compartilhado por todos os profiles)\n# application-dev.properties (só quando o profile \"dev\" está ativo)\n# application-prod.properties (só quando \"prod\" está ativo)": "# application.properties (shared by all profiles)\n# application-dev.properties (only when the \"dev\" profile is active)\n# application-prod.properties (only when \"prod\" is active)",
  "# application-prod.properties (só quando \"prod\" está ativo)": "# application-prod.properties (only when \"prod\" is active)",
  "// injeção, capítulo 22": "// injection, chapter 22",
  "// injeção, capítulo 22!": "// injection, chapter 22!",
  "# ou via variável de ambiente (capítulo 57):": "# or via environment variable (chapter 57):",
  "# ativando um profile:": "# activating a profile:",
  "// 200 OK com o corpo": "// 200 OK with body",
  "// 404, capítulo 26": "// 404, chapter 26",
  "// seu filtro JWT customizado": "// your custom JWT filter",
  "// não envia e-mail de verdade em dev": "// doesn't actually send email in dev",
  "// NUNCA hardcoded de verdade -- vem de variável de ambiente (capítulo 27!)": "// NEVER really hardcoded -- comes from environment variable (chapter 27!)",
  "// desabilitado para APIs stateless com JWT (explicado abaixo)": "// disabled for stateless APIs with JWT (explained below)",
  "/**\").permitAll() // leitura pública": "/**\").permitAll() // public reading",
  "/**\").hasRole(\"ADMIN\")       // só admin": "/**\").hasRole(\"ADMIN\") // admin only",
  "/* SMTP real */ }": "/* real SMTP */ }",
  "// 1 hora": "// 1 hour",
  "// só admin": "// admin only",
  "// público": "// public",
  "// sem sessão!": "// no session!",
  "/* SMTP real */": "/* real SMTP */",
  "// leitura pública": "// public reading",
  "// resto exige login": "// rest requires login",
  "// expirado, assinatura inválida, malformado...": "// expired, invalid signature, malformed...",
  "// hash unidirecional com \"salt\" -- nunca reversível": "// one-way hash with \"salt\" -- never reversible",
  "// só o PRÓPRIO usuário pode editar seu perfil": "// only the user can edit their profile",
  "// checa ANTES do método executar -- outro dynamic proxy, capítulo 20!": "// check BEFORE the method executes -- another dynamic proxy, chapter 20!",
  "-- restrição de valor": "-- value restriction",
  "// ao registrar:": "// when registering:",
  "// gera o JWT": "// generate the JWT",
  "/**\").permitAll()": "/**\").permitAll()",
  "// ao logar:": "// when logging in:",
  "-- CHAVE ESTRANGEIRA -- liga as duas tabelas": "-- FOREIGN KEY -- links the two tables",
  "-- todas as colunas, todas as linhas": "-- all columns, all rows",
  "// compara hash, nunca a senha crua": "// compare hash, never the raw password",
  "-- SERIAL: número que se auto-incrementa": "-- SERIAL: self-incrementing number",
  "-- se algo desse errado no meio, \"ROLLBACK;\" desfaz TUDO desde o BEGIN": "-- if something went wrong in between, \"ROLLBACK;\" undoes EVERYTHING from BEGIN",
  "-- só agora as duas mudanças ficam permanentes, JUNTAS": "-- only now do the two changes become permanent, TOGETHER",
  "-- LEFT JOIN é essencial aqui: com INNER JOIN, autores sem livro nenhum": "-- LEFT JOIN is essential here: with INNER JOIN, authors without any book",
  "-- só retorna livros que TÊM autor correspondente": "-- only returns books that HAVE a corresponding author",
  "-- retorna TODOS os livros, mesmo os sem autor (nome viria NULL)": "-- returns ALL books, even those without an author (name would be NULL)",
  "-- toda coluna usada com frequência em WHERE ou JOIN é candidata a índice": "-- every column frequently used in WHERE or JOIN is a candidate for index",
  "-- LEFT JOIN é essencial aqui: com INNER JOIN, autores sem livro nenhum\n-- simplesmente desapareceriam do resultado": "-- LEFT JOIN is essential here: with INNER JOIN, authors without any book\n-- would simply disappear from the result",
  "-- equivalente ao Collectors.groupingBy(...) + counting() do capítulo 13!": "-- equivalent to Collectors.groupingBy(...) + counting() from chapter 13!",
  "-- crédito": "-- credit",
  "-- débito": "-- debit",
  "-- simplesmente desapareceriam do resultado": "-- would simply disappear from the result",
  "// compartilhado entre TODOS os objetos": "// shared between ALL objects",
  "// static não usa \"this\"": "// static does not use \"this\"",
  "// uso:\n// Pizza p = new PizzaBuilder().tamanho(\"grande\").adicionar(\"mussarela\").adicionar(\"manjericão\").construir();": "// usage:\n// Pizza p = new PizzaBuilder().size(\"large\").add(\"mozzarella\").add(\"basil\").build();",
  "// uso: new PedidoBuilder().item(\"Café\").quantidade(2);": "// usage: new OrderBuilder().item(\"Coffee\").quantity(2);",
  "// uso: Contador.getTotalCriados() -- chamado na CLASSE, sem \"new\"": "// usage: Contador.getTotalCriados() -- called in the CLASS, without \"new\"",
  "// constante de classe": "// class constant",
  "// Pizza p = new PizzaBuilder().tamanho(\"grande\").adicionar(\"mussarela\").adicionar(\"manjericão\").construir();": "// Pizza p = new PizzaBuilder().size(\"large\").add(\"mozzarella\").add(\"basil\").build();",
  "// referência a método de instância \"não vinculado\"": "// reference to \"unbound\" instance method",
  "// só pode ser atribuído uma vez": "// can only be assigned once",
  "// operação intermediária: mantém só alguns": "// intermediate operation: keeps only some",
  "// forma \"clássica\" com classe anônima:": "// \"classic\" form with anonymous class:",
  "// referência a método de instância": "// reference to instance method",
  "// operação intermediária: transforma o tipo": "// intermediate operation: transforms the type",
  "// mesma coisa com lambda -- muito mais direto:": "// same thing with lambda -- much more direct:",
  "// map/filter/collect nada mais são que casos especiais de reduce por baixo dos panos": "// map/filter/collect are nothing more than special cases of reduce under the hood",
  "// esse Postgres sobe do zero antes da classe rodar, e é destruído depois --": "// this Postgres starts from scratch before the class runs, and is destroyed afterwards --",
  "// as migrations do Flyway (capítulo 35) rodam automaticamente nele, exatamente": "// Flyway migrations (chapter 35) automatically run on it, exactly",
  "// sobe um Postgres REAL em Docker, só para esta classe de teste": "// upload a REAL Postgres in Docker, just for this test class",
  "-- repositories,         │": "-- repositories, │",
  "// como rodariam em produção": "// how they would run in production",
  "// esse Postgres sobe do zero antes da classe rodar, e é destruído depois --\n// as migrations do Flyway (capítulo 35) rodam automaticamente nele, exatamente\n// como rodariam em produção": "// this Postgres starts from scratch before the class runs, and is destroyed afterwards --\n// Flyway migrations (chapter 35) automatically run on it, exactly\n// how they would run in production",
  "// operação TERMINAL: dispara o processamento": "// TERMINAL operation: triggers processing",
  "-- o MAIS rápido de todos    │": "-- the fastest of all │",
  "-- controllers, sem banco real       │": "-- controllers, no real bank │",
  "// ferramenta específica de @DataJpaTest,": "// @DataJpaTest specific tool,",
  "// para preparar dados de teste diretamente": "// to prepare test data directly",
  "// ferramenta específica de @DataJpaTest,\n                                                    // para preparar dados de teste diretamente": "// @DataJpaTest specific tool,\n                                                    // to prepare test data directly",
  "// força a escrita real no banco ANTES do teste continuar": "// force actual writing to the database BEFORE the test continues",
  "// se a query SQL gerada realmente ignora capitalização": "// if the generated SQL query really ignores capitalization",
  "// esse teste só faz sentido de verdade contra um Postgres REAL --": "// this test only really makes sense against REAL Postgres --",
  "-- ideal para testar lógica de negócio isolada │": "-- ideal for testing isolated business logic │",
  "// esse teste só faz sentido de verdade contra um Postgres REAL --\n// um mock simplesmente retornaria o que você programasse, sem validar\n// se a query SQL gerada realmente ignora capitalização": "// this test only really makes sense against REAL Postgres --\n// a mock would simply return what you programmed, without validating\n// if the generated SQL query really ignores capitalization",
  "-- mais lento, mas testa a integração completa              │": "-- slower, but tests full integration │",
  "// um mock simplesmente retornaria o que você programasse, sem validar": "// a mock would simply return what you programmed, without validating",
  "// Act (agir)": "// Act (act)",
  "// Assert (verificar)": "// Assert (verify)",
  "// Arrange (organizar)": "// Arrange (arrange)",
  "// NÃO usa H2 --": "// DO NOT use H2 --",
  "// capítulo 42": "// chapter 42",
  "// valores iguais": "// equal values",
  "// NÃO usa H2 --\n                                                                                     usa o Testcontainers real": "// DO NOT use H2 --\n                                                                                     uses real Testcontainers",
  "// sincroniza a espera pela mensagem assíncrona": "// synchronize waiting for asynchronous message",
  "// espera de verdade pela mensagem": "// actually wait for the message",
  "// saldo não deve ter mudado": "// balance must not have changed",
  "// AÇÃO: publica de verdade": "// ACTION: publish for real",
  "/* fechar conexão, apagar arquivo temporário... */": "/* close connection, delete temporary file... */",
  "/* fechar conexão, apagar arquivo temporário... */ }": "/* close connection, delete temporary file... */ }",
  "// roda antes de CADA teste -- evita repetir setup": "// run before EACH test -- avoid repeating setup",
  "// cria um CONSUMER REAL, conectado ao mesmo Kafka em container:": "// creates a REAL CONSUMER, connected to the same containerized Kafka:",
  "// roda depois de CADA teste -- útil para limpar recursos": "// run after EACH test -- useful for cleaning up resources",
  "// 1) RED -- o teste, escrito primeiro:": "// 1) RED -- the test, written first:",
  "// roda 3 vezes, uma para cada valor": "// run 3 times, once for each value",
  "// double: sempre com margem de erro (delta)!": "// double: always with a margin of error (delta)!",
  "// dependência \"falsa\"": "// \"fake\" dependency",
  "// NÃO é atômico! é ler + somar + gravar": "// NOT atomic! is read + add + write",
  "// (ex: usar um array circular) sem medo de quebrar o comportamento.": "// (e.g. using a circular array) without fear of breaking the behavior.",
  "// confirma que o método FOI chamado, sem mandar e-mail de verdade": "// confirms that the method WAS called, without actually sending an email",
  "// forma 1: estender Thread (menos flexível -- Java só permite herança simples)": "// way 1: extend Thread (less flexible -- Java only allows simple inheritance)",
  "// duas threads chamando incrementar() 100_000 vezes cada, ao mesmo tempo,": "// two threads calling increment() 100_000 times each, at the same time,",
  "// 2) GREEN -- implementação mínima para passar:": "// 2) GREEN -- minimum implementation to pass:",
  "// 3) REFACTOR -- com os testes verdes, dá pra trocar a implementação interna\n// (ex: usar um array circular) sem medo de quebrar o comportamento.": "// 3) REFACTOR -- with green tests, you can change the internal implementation\n// (e.g. using a circular array) without fear of breaking the behavior.",
  "// 3) REFACTOR -- com os testes verdes, dá pra trocar a implementação interna": "// 3) REFACTOR -- with green tests, you can change the internal implementation",
  "// forma 2: implementar Runnable (preferida -- permite herdar de outra classe)": "// way 2: implement Runnable (preferred -- allows inheriting from another class)",
  "// duas threads chamando incrementar() 100_000 vezes cada, ao mesmo tempo,\n// frequentemente NÃO resulta em 200_000 -- porque \"valor++\" não é uma\n// operação única: threads podem ler o mesmo valor antes de uma delas gravar.": "// two threads calling increment() 100_000 times each, at the same time,\n// often does NOT result in 200_000 -- because \"value++\" is not a\n// single operation: threads can read the same value before one of them writes.",
  "// start(), NUNCA run() diretamente!": "// start(), NEVER run() directly!",
  "// operação única: threads podem ler o mesmo valor antes de uma delas gravar.": "// single operation: threads can read the same value before one of them writes.",
  "// ou um bloco sincronizado sobre um objeto específico -- mais granular:": "// or a synchronized block over a specific object -- more granular:",
  "// AtomicInteger: incremento atômico sem precisar de synchronized manual": "// AtomicInteger: atomic increment without needing manual synchronized",
  "// frequentemente NÃO resulta em 200_000 -- porque \"valor++\" não é uma": "// often does NOT result in 200_000 -- because \"value++\" is not a",
  "// ExecutorService: um pool de threads gerenciado, em vez de \"new Thread()\" solto": "// ExecutorService: a managed thread pool, instead of loose \"new Thread()\"",
  "// sempre encerre o pool quando não precisar mais dele": "// always close the pool when you no longer need it",
  "// bloqueia até essa tarefa terminar": "// block until this task finishes",
  "// tarefa que retorna um valor": "// task that returns a value",
  "// só uma thread por vez entra aqui": "// only one thread at a time enters here",
  "// thread-safe de verdade": "// truly thread safe",
  "// 200000, sempre, graças ao synchronized": "// 200000, always, thanks to synchronized",
  "// bloqueia até o resultado ficar pronto": "// block until the result is ready",
  "// só uma thread por vez executa isto": "// only one thread at a time executes this",
  "// espera as duas terminarem antes de continuar": "// wait for both to finish before continuing",
  "// disparada por @Valid quando falha": "// triggered by @Valid when failure",
  "// narrowing: vira 7, não arredonda": "// narrowing: becomes 7, does not round",
  "//viacep.com.br\").build();": "//viacep.com.br\").build();",
  "// sua exceção do capítulo 17!": "// your exception from chapter 17!",
  "// widening: seguro": "// widening: safe",
  "// streams, capítulo 13": "// streams, chapter 13",
  "// capítulo 55!": "// chapter 55!",
  "// CORS, capítulo 50, aplicado aqui também": "// CORS, chapter 50, applied here too",
  "// cliente manda para /app/emprestimo": "// client sends to /app/emprestimo",
  "// servidor retransmite para todos inscritos": "// server retransmits to all subscribers",
  "// canais que o servidor publica para": "// channels that the server publishes to",
  "// prefixo para mensagens do cliente": "// prefix for client messages",
  "// se a validação falhar, o método NUNCA é executado --": "// if validation fails, the method is NEVER executed --",
  "// o Spring já responde 400 Bad Request antes de chegar aqui": "// Spring already responds 400 Bad Request before arriving here",
  "// se a validação falhar, o método NUNCA é executado --\n    // o Spring já responde 400 Bad Request antes de chegar aqui": "// if validation fails, the method is NEVER executed --\n    // Spring already responds 400 Bad Request before arriving here",
  "// rede de segurança -- NUNCA deixe stack trace vazar": "// safety net -- NEVER let stack trace leak",
  "// versão síncrona simples -- .block() espera o resultado": "// simple synchronous version -- .block() waits for result",
  "//viacep.com.br/ws/{cep}/json\", EnderecoDTO.class, cep);": "//viacep.com.br/ws/{cep}/json\", EnderecoDTO.class, cep);",
  "//localhost:5173\"); // CORS, capítulo 50, aplicado aqui também": "//localhost:5173\"); // CORS, chapter 50, applied here too",
  "// atualiza a UI sem recarregar a página": "// update the UI without reloading the page",
  "// front-end (usando a biblioteca STOMP sobre SockJS):": "// front-end (using STOMP library over SockJS):",
  "// ... lógica de empréstimo já existente (capítulo 17/22) ...": "// ... existing loan logic (chapter 17/22) ..."
}